# Tourya — Documentación maestra

Este directorio contiene la **documentación de referencia** de la plataforma Tourya, generada a partir del código real (backend Spring Boot, frontend Angular, app MAUI Android, base de datos PostgreSQL, infraestructura GCP).

**Objetivo**: que cualquier desarrollador, PM, agente IA o stakeholder nuevo pueda leer estos documentos en orden y entender el sistema sin necesidad de explicaciones verbales.

---

## Cómo usar esta documentación

- **Leer en orden numérico la primera vez** (00 → 13). Toma ~2–3 horas.
- Después usar como referencia.
- Cualquier cambio en una regla de negocio, entidad o decisión técnica **se documenta primero acá** y después se implementa en código.
- Si el código contradice la doc, la doc gana (corregimos código) — salvo cuando el código ya está en producción, en cuyo caso se actualiza la doc.

---

## Convención de marcas

Cada afirmación en estos documentos está marcada con su origen:

| Marca | Significado |
|-------|-------------|
| ✅ **DEDUCIDO DEL CÓDIGO** | Inferido leyendo el código fuente, migraciones, configuraciones |
| 📌 **PENDIENTE LUIS** | Requiere validación o información del cliente (PO) |
| ❓ **ASUNCIÓN** | Hipótesis basada en el dominio turístico — confirmar con Luis |
| ⚠️ **RIESGO / TECH DEBT** | Algo que requiere atención (vulnerabilidad, código viejo, configuración insegura) |

---

## Índice

### Fundamentos del negocio

| # | Documento | De qué trata |
|---|-----------|--------------|
| [01](01-vision-y-negocio.md) | Visión y negocio | Qué es Tourya, mercado objetivo (Colombia turismo), monetización, modelo de comisión |
| [02](02-glosario.md) | Glosario | Diccionario de términos: tour, schedule, slot, operador, RNT, Wompi, override, payout |
| [03](03-roles-y-actores.md) | Roles y actores | USER, PROVIDER, PROVIDER_OPERATOR, ADMIN, BACKOFFICE_OPERATION, AUTH externos |

### Dominio y reglas

| # | Documento | De qué trata |
|---|-----------|--------------|
| [04](04-entidades-dominio.md) | Entidades de dominio | 54 entidades JPA agrupadas en 10 bounded contexts, con relaciones |
| [05](05-reglas-de-negocio.md) | Reglas de negocio | Comisión Tourya, cancelaciones/refund, overrides, créditos, payouts, KYB, activación |
| [06](06-flujos-y-eventos.md) | Flujos y eventos | 8 flujos críticos: registro, creación de tour, schedule + precios, cart, checkout Wompi, cancelación, payout, KYB |

### Arquitectura

| # | Documento | De qué trata |
|---|-----------|--------------|
| [07](07-arquitectura-tecnica.md) | Arquitectura técnica | Stack: Spring Boot 3.4.4 + Angular 19 + MAUI .NET 10 + PostgreSQL + GCP (Cloud Run, Cloud SQL, GCS, Load Balancer) |
| [08](08-modelo-de-datos.md) | Modelo de datos | Schema PostgreSQL: 68 tablas, 61 migraciones, 20 SPs, JSONB para i18n, índices |

### Interfaces

| # | Documento | De qué trata |
|---|-----------|--------------|
| [09](09-api-design.md) | API design | 150+ endpoints en 35 controllers, convenciones REST, autenticación JWT, multipart |
| [10](10-mobile-spec.md) | Mobile spec (MAUI) | App Android: 25 vistas, 26 ViewModels, Syncfusion, Wompi WebView, QR scanner |

### Operación y entrega

| # | Documento | De qué trata |
|---|-----------|--------------|
| [11](11-integraciones.md) | Integraciones | Wompi (pagos), SMTP Gmail Workspace, GCS/S3 (storage), Firebase Auth (Google/Facebook), Google Translation (futuro) |
| [12](12-seguridad-y-auth.md) | Seguridad y autenticación | JWT, BCrypt, roles, login social, CORS, vulnerabilidades pendientes |
| [13](13-despliegue-cicd.md) | Despliegue GCP y CI/CD | Cloud Run, Cloud Build, GitHub Actions, ramas, dominios (tourya.co), variables de entorno |

### Análisis y roadmap

| # | Documento | De qué trata |
|---|-----------|--------------|
| [14](14-gap-web-mobile.md) | Análisis Gap Web vs Mobile | Qué debe existir en cada plataforma y por qué — evitar duplicar trabajo que no aporta valor |
| [15](15-mvp-mobile-estado.md) | Estado del MVP mobile vs alcance | Gap analysis granular del código MAUI actual contra el alcance del doc 14 + roadmap por ciclos |
| [16](16-agentes-ia.md) | Agentes IA | Travel Concierge, moderador de reseñas y otros agentes IA del roadmap (📌 pendiente contenido de Luis) |
| [17](17-backlog-implementacion.md) | Backlog de implementación y plan de ejecución | Backlog priorizado (P0–P3) por área + plan por fases + cronograma + riesgos |

---

## Status de actualización

Generación inicial: **2026-06-27**. Consolidación con aportes de Luis: **2026-06-28**.

Documentos basados en el estado del código en las ramas:

| Repo | Rama | Commit |
|------|------|--------|
| tourya-api | `develop` | `e6ecca7` |
| tourya-front | `develop` | `983f73a` |
| tourya-mobile | `master` | `0bc518f` (sin remoto GitHub) |

| Doc | Status | Notas |
|-----|--------|-------|
| 00 README maestro | ✅ Sync | Este índice |
| 01 Visión y negocio | ✅ Sync | Consolidado con aportes de Luis (mercado, metas 12 meses, roadmap) |
| 02 Glosario | ✅ Sync | Términos y aclaraciones de Luis integrados |
| 03 Roles y actores | ✅ Sync | ADMIN único, BACKOFFICE_OPERATION asignable, matriz corregida |
| 04 Entidades de dominio | ✅ Sync | 54 entidades; `TourReservation` legacy eliminado (commit `076f006`) |
| 05 Reglas de negocio | ✅ Sync | Políticas de cancelación, refresh tokens por rol, KYB docs, moderación IA en roadmap |
| 06 Flujos y eventos | ✅ Sync | 8 flujos completos |
| 07 Arquitectura técnica | ✅ Sync | Stack y deploy confirmado |
| 08 Modelo de datos | ✅ Sync | 68 tablas, 20 SPs, 61 migraciones + refactor pendiente `Tour.percentageTourya` |
| 09 API design | ✅ Sync | 150+ endpoints en 35 controllers |
| 10 Mobile spec | ✅ Sync | Estructura MAUI mapeada |
| 11 Integraciones | ✅ Sync | Wompi (webhook en roadmap), SMTP, GCS, Firebase |
| 12 Seguridad y autenticación | ⚠️ Críticos pendientes | Refresh tokens por rol definidos; vulnerabilidades en plan aparte |
| 13 Despliegue GCP y CI/CD | ✅ Sync | Pipelines AWS (legacy) + GCP confirmados |
| 14 Web vs Mobile — alcance | ✅ Sync (v2.0) | Reescrito con el alcance definido por Luis: mobile cubre crear tour, schedule y operarios porque los operadores están en la calle |
| 15 MVP mobile — estado | ✅ Sync | Gap analysis granular actualizado según el nuevo alcance del doc 14 |
| 16 Agentes IA | ✅ Sync (v1.0) | Luis subió contenido completo: 5 agentes core (Travel Concierge, Support 24/7, Desert Shopping Cart, Operator Support, Backoffice Support) + arquitectura + roadmap |
| 17 Backlog de implementación | ✅ Sync (v1.1) | Consolida trabajo pendiente en 6 áreas + plan por fases con estimaciones **agente IA + review humano** (~3 semanas hasta MVP, ~10 semanas hasta Fase 4) |

---

## Documentos relacionados (fuera de este directorio)

Estos documentos viven fuera del repo (en `D:/Users/Usuario/source/repos/tourya/`) por ser internos o sensibles:

| Documento | Ubicación | Naturaleza |
|-----------|-----------|------------|
| Plan de remediación de seguridad | `../../security-remediation-plan.md` | **Interno** — mapa de vulnerabilidades, no compartir |
| Plan de mejora CI/CD | `../../cicd-improvement-plan.md` | Interno — JaCoCo, SpotBugs, Snyk |
| Propuesta social login sin Firebase | `../../social-login-google-facebook.md` | Para cliente |
| Propuesta traducción automática | `../../traduccion-automatica-tours.md` | Para cliente |

---

## Convenciones de la documentación

- **Idioma**: español (mismo idioma del cliente y del dominio turístico colombiano).
- **Tono**: directo, sin academicismos. Mejor "el proveedor crea el tour" que "el actor de tipo proveedor genera el evento de creación del agregado tour".
- **Ejemplos concretos**: cuando sea posible, con nombres reales del dominio (Cartagena, Islas del Rosario, RNT 263565).
- **Referencias cruzadas**: si un doc menciona una entidad, link al doc donde se define.
- **Decisiones marcadas explícitamente** con `> **Decisión**: X — porque Y` para que sean fácil de auditar.

---

## Mantenimiento

Estos documentos son **vivos**. Reglas:

1. **Cambio menor** (typo, aclaración): editar directo + commit.
2. **Cambio de regla de negocio**: actualizar el doc + agregar entrada en changelog del doc + verificar que el código aún sea consistente.
3. **Cambio de arquitectura o entidad nueva**: discusión con equipo + actualización doc + plan de migración si afecta datos existentes.

Nadie escribe código que contradiga estos documentos sin antes actualizar el documento.

---

## Log de decisiones

Bitácora cronológica de decisiones aprobadas por el equipo. Sirve como registro rápido del "por qué" cuando el tiempo pasa y las decisiones parecen obvias.

| Fecha | Decisión | Aprobado por | Referencia |
|-------|----------|--------------|------------|
| 2026-06-28 | Meta iso-funcionalidad web/mobile ajustada: mobile cubre el ciclo operativo completo del proveedor (operadores en la calle en San Andrés) | Luis | [14](14-gap-web-mobile.md) |
| 2026-06-28 | `TourReservation` legacy eliminado | Equipo | migración 065 (commit `076f006`) |
| 2026-06-28 | Nuevas razones de cancelación: `LEGAL_OBLIGATIONS`, `CHANGE_OF_PLANS` | Luis | RN-030 |
| 2026-06-28 | Políticas de cancelación: Flexible/Estándar/Moderado/Estricto (100% refund si se cumple) | Luis | RN-031 |
| 2026-07-07 | `isUnlimitedCapacity` es LIMPIEZA (eliminar de `tour_schedule`), no migración — la fuente de verdad está en `Tour` | Luis | RN-021 |
| 2026-07-07 | Nuevo campo `Tour.percentageTourya` (default de comisión al aprobar el tour) | Luis | RN-015 |
| 2026-07-07 | `holdMinutes` del carrito debe ser configurable por ADMIN vía `app_config` | Luis | RN-022 |
| 2026-07-07 | Avanzar con **webhook server-side de Wompi** — prioridad alta para no perder pagos | Luis | RN-025 |
| 2026-07-07 | Refresh tokens diferenciados por rol (USER 60m/30d, PROVIDER 30m/7d/4h idle, ADMIN 15m/8h/15m idle). Rotación estricta + detección de reuso | Franklin | RN-005 |
| 2026-07-07 | WhatsApp vía **Twilio** (no integración directa con Meta) — velocidad de implementación sobre costo | Luis + Franklin | [16](16-agentes-ia.md), [11](11-integraciones.md) |
| 2026-07-07 | Tabla `agent_audit_log` es prerequisito para dar autonomía a cualquier agente IA | Franklin | [16](16-agentes-ia.md) |
| 2026-07-07 | Alertas básicas de Cloud Monitoring son prerequisito antes de salir a producción | Franklin | [13](13-despliegue-cicd.md) |
| 2026-07-07 | Presupuesto mensual agentes IA: **$100–200 USD/mes** para inferencia LLM | Luis | [16](16-agentes-ia.md) |
| 2026-07-08 | **Fase 0 higiene y seguridad crítica ejecutada en dev**: 5 fixes de código (SEC-03 Actuator, SEC-04 token replay, SEC-05 PII leak, SEC-08 password log, SEC-11 CORS) | Franklin | PRs #146–#150 |
| 2026-07-08 | **Migración de secretos runtime a GCP Secret Manager** en `tourya-project-dev`: JWT (rotado), Wompi, DB, SMTP. Fallbacks en `application.properties` como puente. | Franklin | PRs #151, #152 |
| 2026-07-08 | **Autenticación GitHub Actions → GCP migrada a Workload Identity Federation (WIF)**. Ya no se usan service account keys JSON. Key user-managed peligroso `df67...` deshabilitado. GitHub Secret `GCP_SA_KEY_DEV` eliminado. | Franklin | PR #153 |
| 2026-07-08 | **7 alertas de Cloud Monitoring activas** en dev (5xx backend/frontend, latencia P95, Cloud SQL CPU/disk/connections, container CPU). Canal: email a owner. Cost anomaly (8ª) queda pendiente por requerir Billing Budget separado. | Franklin | INF-01 |
| 2026-07-08 | **Backups automáticos de Cloud SQL habilitados** en dev: diarios 03:00 UTC, retention 30 días, multi-region `us`, Point-in-Time Recovery activo (7 días de transaction logs). | Franklin | INF-02 |
| 2026-07-08 | **🎉 Fase 0 completa en dev**: 5 fixes de código, Secret Manager, WIF, alertas y backups. Solo pendiente: cost anomaly alert (billing budget). Listos para arrancar Fase 1 (MVP core). | Franklin | Fase 0 cierre |
| 2026-07-08 | QR baseUrl AWS legacy resuelto — `ReservationQrService` lee de env var con fallback a `https://tourya.co/home` | Franklin | PR #154 |
| 2026-07-08 | Fallbacks hardcoded de JWT y Wompi secrets **removidos** de `application.properties`. Fail-fast en producción: si Cloud Run pierde acceso a Secret Manager, la app falla al arrancar en vez de usar valores viejos | Franklin | PR #155 |
| 2026-07-08 | **Fase 1 arrancada — BE-16/17 Wompi webhook + reconciliación operacional en dev**: endpoint `POST /public/wompi/webhook` verifica firma HMAC-SHA256 con events secret, persiste todos los eventos en tabla `wompi_webhook_event`. Job cada 5 min matchea contra Payments y detecta pagos huérfanos. URL configurada en dashboard Wompi Sandbox por Luis. Verificado end-to-end. | Franklin | PRs #156 + #157 + #158 |
| 2026-07-08 | **BE-12/13/14/15 refresh tokens implementados**: nueva tabla `refresh_token` + endpoints `/auth/refresh` y `/auth/logout` + rotación con detección de reuso + logout server-side. Backwards compat: response de `/auth/authenticate` mantiene `token` (legacy alias) + agrega `accessToken` y `refreshToken`. Angular/MAUI existentes siguen funcionando sin cambios. | Franklin | PR #159 |
| 2026-07-09 | **BE-06/07/08/09 configs a `app_config`**: 3 valores hardcoded (holdMinutes, buffer payout, expiración crédito) migrados a la tabla `app_config` con endpoint `PUT /config/{key}` protegido con `@PreAuthorize("hasRole('ADMIN')")`. ADMIN puede ajustar sin re-deploy. Corrección detectada: expiración de créditos usa 6 meses en código (no 1 año como decía el backlog); se mantuvo 6 como default. | Franklin | PR #160 |
| 2026-07-09 | **BE-10 validador RN-013 en galería** aplicado en `POST /tours/{id}/gallery/sync` y `/syncWithUpdate`. All-or-nothing: rechazo con lista de issues + rollback antes de tocar S3. 3 keys nuevas en `app_config` (`GALLERY_MAX_SIZE_MB`, `GALLERY_MIN_WIDTH_PX`, `GALLERY_MAX_IMAGES_PER_TOUR`). Decisión de diseño: tocar los métodos existentes en vez de crear endpoint alterno ("no hay cosas más permanentes que el mientras tanto"). Descartado "ancho recomendado 1920px" por ser solo recomendación. | Franklin | PR #161 |
| 2026-07-09 | **BE-11 RN-045 KYB docs obligatorios con feature flag OFF**: `PUT /requestProvider/user/send` valida (cuando flag=1) que existan galleries adjuntas para los 7 `RequestProviderDocumentType` con `mandatory=true` (Cámara comercio, RUT, RNT, Cédula RL, Seguros operación, Contrato mandato, Contrato vinculación). Response 400 con `missingDocuments[]`. Default OFF para no bloquear los 12 requests en estado Created de QA. Descubrimiento adicional: hay ~5 providers ya aprobados históricamente con solo 2-3 documentos — se resuelven manualmente si Luis lo pide. | Franklin | PR #162 |
| 2026-07-09 | **BE-01/02 RN-015 default % Tourya por tour**: reactivado `tour.porcentaje_tourya` (existía como DEPRECATED desde 050 según decisión previa). Backfill de 38 tours a 0.15 + DEFAULT column = 0.15. `TourScheduleConfigGeneralService` en 2 puntos hereda del tour al crear slot nuevo (antes hardcode ZERO). Nombre queda en español — rename descartado por costo/valor. | Franklin | PR #163 |
| 2026-07-09 | **SEC-09 rate limiting en `/auth/**` con feature flag OFF**: `AuthRateLimitFilter` in-memory por IP, umbral configurable en `app_config` (60/min default). Se activa sin re-deploy con `PUT /config/AUTH_RATE_LIMIT_ENABLED`. Limitación: contador no coordinado entre réplicas de Cloud Run (max-instances=2 → hasta 2×). Aceptable como defensa en profundidad; migrar a Redis si Luis pide coordinación estricta. | Franklin | PR #164 |
| 2026-07-09 | **SEC-10 account lockout con backoff exponencial y feature flag OFF**: 2 columnas nuevas en `_user` (`failed_login_attempts`, `locked_until`). Al superar `AUTH_LOCKOUT_MAX_ATTEMPTS` (5 default), la cuenta se bloquea con backoff exponencial que dobla el tiempo por cada nuevo fallo (60s → 120s → 240s → ... cap 24h). Auto-unlock via `isAccountNonLocked()` cuando `locked_until < now()` — sin cron. Silencioso si el email no existe (evita filtrar cuentas). Complementa SEC-09: rate limit por IP + lockout por cuenta. H-4 resuelto. | Franklin | PR #165 |
| 2026-07-09 | **BE-05 nuevas razones de cancelación**: `LEGAL_OBLIGATIONS` y `CHANGE_OF_PLANS` agregadas al enum `CancellationReasonEnum` (RN-030). Sin migración SQL — la columna `reservation.cancellation_reason` es VARCHAR(20) y los strings caben. Frontend/Mobile (FE-06) debe agregar las opciones al dropdown para que el turista pueda elegirlas desde la UI. | Franklin | PR #166 |
| 2026-07-09 | **FE-06 nuevas razones cancelación en Angular**: cierra el bucle end-to-end del BE-05. `provider-tour-management` dropdown + `formatCancellationReason` + validación + i18n en es/en/pt. Fix colateral: `INABILITY_TO_TRAVEL` faltaba en el mapping (bug preexistente). El código quedó en `develop` pero el deploy inicial falló (ver entrada del 2026-07-10). | Franklin | PR tourya-front #56 |
| 2026-07-10 | **Migración de tourya-front a Workload Identity Federation**: al mergear FE-06 se descubrió que el workflow de tourya-front seguía usando el secret `GCP_SA_KEY_DEV` que Franklin eliminó cuando migramos backend a WIF (2026-07-08). Todos los deploys del front fallaban silenciosamente desde entonces — la última revisión activa era del 2026-07-02. Fix: aplicar el mismo patrón WIF que ya existía en backend + agregar `Touryapp/tourya-front` al IAM binding del SA. Nueva revisión `tourya-dev-front-00024-b6m` activa; FE-06 finalmente en producción de dev. Se agrega `workflow_dispatch` al workflow para poder disparar deploys manualmente. | Franklin | PR tourya-front #57 |
| 2026-07-10 | **FE-04 panel ADMIN de `app_config` en Angular**: nuevo `AppConfigAdminComponent` (standalone) integrado al Dashboard siguiendo el patrón `toggleX` del sidebar (no rutas nuevas). Edita en caliente 12 configs en 5 grupos: Reservas (HOLD_MINUTES), Payouts/Créditos (PAYOUT_BUFFER_DAYS, CREDIT_EXPIRATION_MONTHS), Galería (GALLERY_MAX_SIZE_MB, GALLERY_MIN_WIDTH_PX, GALLERY_MAX_IMAGES_PER_TOUR), KYB (KYB_REQUIRE_MANDATORY_DOCS), Auth (AUTH_RATE_LIMIT_ENABLED/PER_MINUTE, AUTH_LOCKOUT_ENABLED/MAX_ATTEMPTS/BASE_BACKOFF_SECONDS). Carga en paralelo con `forkJoin` + `catchError` por item; save/reset por item; i18n es/en/pt. `CANCELLATION_POLICY` queda fuera del v1 (requiere editor JSON dedicado). Revisión desplegada: `tourya-dev-front-00025-s7q`. | Franklin | PR tourya-front #58 |
| 2026-07-10 | **FE-03 validador pre-upload de galería en Angular**: `tour-gallery.component` (PROVIDER) ahora valida MIME (JPEG/PNG/WebP), tamaño, orientación landscape, ancho mínimo y cuenta total (existentes + nuevas) antes de mandar al backend. Umbrales leídos de `app_config` vía `AppConfigService` con fallback a defaults; si ADMIN sube un límite desde el panel FE-04, el uploader lo respeta sin re-deploy. Los `issues[]` del 400 del backend se muestran en el mismo modal Swal (bypass o cambio server-side). Backend BE-10 sigue como defensa en profundidad — el frontend solo mejora UX evitando el round-trip. Revisión desplegada: `tourya-dev-front-00026-rjh`. | Franklin | PR tourya-front #59 |
| 2026-07-10 | **FE-01 se implementa vía localStorage (opción B), NO cookie HttpOnly**: el backlog original decía "guardar refresh_token en cookie HttpOnly + auto-refresh transparente". Para ir por HttpOnly hay que tocar `/auth/authenticate`, `/auth/refresh` y `/auth/logout` en el backend para setear/leer cookies + habilitar CORS `credentials:true` + `withCredentials:true` en Angular — scope grande y con riesgo alto en el login (neurálgico). Se decide **opción B**: guardar `accessToken` + `refreshToken` en `localStorage` (JSON body como ya devuelve el backend BE-12/13/14/15), auto-refresh con mutex en el `AuthInterceptor`, `AuthService.logout()` que llama server-side. Menos seguro contra XSS que HttpOnly pero cierra el bucle FE↔BE sin tocar backend. El endurecimiento HttpOnly queda como item futuro **FE-01b** (Fase 2 de seguridad). | Franklin | PR tourya-front #60 |
| 2026-08-14 | **IA-02 Travel Concierge backend cerrado + cambio de LLM provider a Vertex AI Gemini**. Primer agente real del bloque IA (doc 16 §Agente 1). Endpoint `POST /agents/travel-concierge/chat` (JWT USER) implementado con `TravelConciergeService` que corre un loop de function calling (max 5 iter) contra `TourService`/`ShoppingCartService`/`SearchTourScheduleFullService` — 4 funciones expuestas: `search_tours`, `get_tour_detail`, `add_to_cart`, `get_cart`. **Cambio arquitectural**: se cambia el LLM provider de Anthropic Claude a **Vertex AI Gemini** — reusa infra GCP existente (SA `tourya-dev-cloud-run` con `roles/aiplatform.user`, cero API key nueva), ~40% mas barato ($1.25/$10 Gemini 2.5 Pro vs $2/$10 Claude Sonnet 5), y el framework `ILlmClient` ya era provider-agnostic (IA-01) — el switch es estrictamente aditivo, `AnthropicClient` queda como impl alternativa via `AGENTS_PROVIDER=anthropic`. Auth vertex ai via ADC (SA en Cloud Run, `gcloud auth application-default login` en local). Nuevo `GeminiClient` + `LlmClientConfig` con `@Primary` selector, nuevas entradas en `ModelPricing` (Gemini 2.5 Pro y 2.5 Flash), 3 nuevas config keys `agents.provider/gemini.enabled/gemini.project-id/gemini.location` en `application.properties`. **Guardrails en codigo (no solo prompt)**: deny-list de secretos (`WOMPI_INTEGRITY_SECRET`/`JWT_SECRET`/`ANTHROPIC_API_KEY`/etc.) en `userMessage` → escala humano + audit `rejected`; scrub recursivo de `providerPrice`/`slotPercentageTourya`/variantes ES del contexto pre-envio (defense-in-depth aunque los DTOs actuales ya no los exponen); contador in-memory de pagos fallidos por `sessionId` con umbral 3 → `fraud_suspected=true` en metadata (nunca comunicado al turista). Migracion 092 agrega `metadata JSONB` + GIN index a `agent_audit_log` (session_id, fraud_suspected, actions_executed[], escalated_to_human). 9 unit tests JUnit + Mockito con nuevo `MockLlmClient` (queue de responses hardcoded) cubriendo los 5 escenarios del brief (consulta simple, `search_tours`, `add_to_cart`, guardrail secretos, scrub) + budget guard + fraud counter. Costo estimado recalculado con Gemini: **$8-12/mes** para Travel Concierge (contra $20-25 con Anthropic). PR pending merge. Migracion 092 NO aplicada a Cloud SQL dev — Franklin la corre con el patron docker+psql. | Franklin | PR tourya-api #<pendiente> |
| 2026-07-17 | **MO-50 + MO-51 (operario mobile) cerrados en bloque**. Cierra el bloque operario del backlog mobile. **MO-50 offline cache**: nuevo `IReservationCacheService` con JSON en `FileSystem.CacheDirectory` (sin SQLite, sin dep nueva). `DashboardViewModel` implementa save-on-success + fallback-on-error con banner amarillo "📴 Modo offline · última actualización hace X min". Escenario típico: operator en San Andrés en muelle con señal débil abre la app → Dashboard muestra las reservas del día desde cache en vez de pantalla vacía. **MO-51 vista mejorada**: `ProviderReservationsViewModel` rehecho — chips filtro por tour (auto-derivados de las reservas cargadas), agrupación por día con `CollectionView.IsGrouped` + nuevo `ReservationDayGroup`, ordenamiento ascendente por `ScheduleDate + SlotTimeStart`. Filtro y agrupación puramente client-side. Código mobile sigue en local (MO-00 postponed). | Franklin | tourya-mobile local |
| 2026-07-16 | **IA-01 framework shared de agentes IA cerrado + rescate de IA-00**. Descubierto al empezar IA-01: el commit `2481d32` de IA-00 mergeado ayer no llegó a `origin/develop` (algo pasó con el PR — Franklin canceló o hubo conflict silencioso). Se recuperó via `git reflog` + cherry-pick a la rama de IA-01 — un único PR trae ambos commits juntos. **Framework shared** (`com.tourya.api.agents.shared`): `ILlmClient` interface + `AnthropicClient` (RestTemplate + Anthropic Messages API v1, no-op degradado sin `ANTHROPIC_API_KEY`), `AgentLlmResponse` record con costo pre-calculado, `ModelPricing` (Sonnet 5 $2/$10, Haiku 4.5 $1/$5, Opus 4.8 $15/$75, fallback conservador para modelo desconocido), `PromptTemplate` (classpath + placeholders `{{var}}`), `AgentRunResult<T>` sealed (`autonomousAction`/`suggestion`/`rejected`/`error`), `AgentAuditEntry` builder + `AgentAuditWriter @Async` (nunca propaga fallos — el negocio ya actuó), `BudgetGuard` (v1 aproximación calls×$0.01 hasta que el repo tenga SUM real — deuda IA-01b registrada). 6 keys nuevas en `ConfigKeyEnum` para caps por agente (defaults $10-$30/mes). 13 unit tests JUnit+Mockito (34 total). Config: `agents.anthropic.api-key` (env var) + timeouts 10s conn/30s read en `RestTemplate` bean. **Franklin sube `ANTHROPIC_API_KEY` a Secret Manager (`tourya-anthropic-api-key`) cuando quiera activar** — mismo patrón que Firebase Admin SDK. PR pending merge. | Franklin | PR tourya-api #<pendiente> |
| 2026-07-15 | **IA-00 tabla `agent_audit_log` creada — arranca bloque IA**. Prerequisito de todo el bloque agentes IA (Principio rector #4 del doc 16): auditoría obligatoria de cada llamada de un agente al LLM. Migración 076 con 16 columnas (agent_name, model, prompt_version, input/output_tokens, cost_usd, duration_ms, entity_type + entity_id sin FK cross-domain, user_id, result_type, result_json jsonb, prompt_input, error_message, created_at) + 3 índices (agent+date DESC para dashboards, entity_type+entity_id para forense, user+date DESC para historial). Entity JPA `AgentAuditLog` con `@JdbcTypeCode(SqlTypes.JSON)` para jsonb (patrón consistente con `WompiWebhookEvent`). `AgentAuditLogRepository` con 4 queries típicos + `countByAgentNameAndCreatedAtAfter` para el `BudgetGuard` de IA-01. Sin service ni endpoint — eso queda para IA-01 (paquete shared con `AgentAuditWriter`). Compile OK. PR pending merge. | Franklin | PR tourya-api #<pendiente> |
| 2026-07-15 | **MO-31 + MO-34 (refinamiento provider mobile) cerrados en bloque**. (1) **MO-31 compresión imágenes cliente**: nuevo NuGet `SkiaSharp` 3.116.1 + `IImageCompressionService` (redimensiona a 1920px max lado largo + JPEG 80). Integrado en `CreateReviewViewModel.AddPickedImageAsync` (fotos de reseña). Reducción típica 3-8 MB → 300-800 KB (~85% ahorro), uploads 5-10x más rápidos, menor costo GCS. Fail-safe: si el compresor falla (formato raro, corrupto), devuelve el stream original. TourFormPage no sube imágenes desde mobile; KYB usa FilePicker para PDFs (compresión no aplica). (2) **MO-34 copiar de plantilla**: cambio de scope — el response `/tour-schedules/templates` del backend NO incluye `tourId`, sin tocar backend no se puede filtrar por tour. Renombrado a "Copiar de otra plantilla" (todas las del provider) — mejor UX igual porque el provider suele tener varias plantillas (`fin de semana`, `semana`, etc.). Botón "📋 Copiar" en `ScheduleTemplateFormPage` → `DisplayActionSheet` con label + días → confirm reemplazo → aplica slots+precios de la fuente. Código mobile sigue en local. | Franklin | tourya-mobile local |
| 2026-07-15 | **FE-11 i18n de `/providers/requestproviders` cerrada**. Cierra la deuda registrada esta misma mañana tras el reporte de Luis (la pantalla estaba en inglés hardcoded). Nueva sección `request-provider` en `public/i18n/{es,en,pt}.json` con ~50 claves (breadcrumb, sidebar, loading, sections, fields+placeholders, validation, buttons con interpolación `{{name}}`, status, documents con workflow/badges/summary/formats, cancelled). HTML del componente reemplazado — placeholders migrados a `[placeholder]` attribute binding para soportar pipes. `tabs[].label` en el TS pasó de strings hardcoded ("Basic Info", "Documents") a claves i18n. Sin cambios funcionales — solo idioma. `SharedModule` ya exponía `TranslateModule`. Los 3 JSON validados con `JSON.parse`. Rama merged tourya-front. | Franklin | PR tourya-front |
| 2026-07-15 | **MO-43 deep-linking WhatsApp cerrado**. (1) Botón 🔗 en `TourDetailPage` → `Share.Default.RequestAsync` con mensaje `"¡Mira este tour en Tourya! [nombre] [url]"` → share sheet nativo (WhatsApp/SMS/email/etc.). (2) `MainActivity` con `[IntentFilter]` captura links `https://dev.tourya.co/clients/tours-detail/*`. `HandleDeepLink()` invocado en `OnCreate` (cold start) + `OnNewIntent` (app viva) parsea URI y navega a Shell `tour-detail?tourId=X`. Sin `autoVerify` — Android muestra chooser (deuda **MO-43b**: montar `.well-known/assetlinks.json` en `dev.tourya.co` cuando queramos "abrir directo" sin preguntar). Nuevo `Constants.WebBaseUrl` + `Constants.WebHost` — single point of change para migrar a `tourya.co` en prod. Web ya tiene la ruta `/clients/tours-detail/:id` públicamente accesible (fallback si el receptor no tiene la app). | Franklin | tourya-mobile local |
| 2026-07-15 | **MO-42 + MO-41 (geo mobile) cerrados en bloque**. Dos features nativas del mobile que web no puede replicar. **MO-42**: sección "Punto de encuentro" en `TourDetailPage` con dirección + botón "Cómo llegar" que abre la app de mapas default del sistema (Google Maps/Waze) vía `Map.Default.OpenAsync` — respeta la preferencia del user. **MO-41**: chip toggle "📍 Cerca de mí" en `ExplorePage` con permiso runtime on-demand, Haversine client-side, ordena tours por distancia y muestra "X km" en cada card. Nuevo `ILocationHelperService` (wrapper con timeouts + cache 5min de last-known) + `DistanceCalculator` (static, Haversine). Permisos Android `ACCESS_FINE_LOCATION`+`ACCESS_COARSE_LOCATION` agregados. Backend intacto — coords ya venían en `TourAddress.latitude/longitude`. Sin embedded map de Syncfusion (queda como MO-42b futuro si Luis lo pide — para v1 el botón resuelve el 100% del caso de uso). Código mobile sigue en local. | Franklin | tourya-mobile local |
| 2026-07-15 | **MO-21 PayoutsPage provider mobile cerrada**. Cierra el ciclo económico del provider en mobile: ahora puede ver sus payouts, filtrar por status, y descargar comprobantes desde el celular (antes tenía que ir al web). Piezas: `PayoutOrderService` (2 métodos análogos a los endpoints del backend, sin cambios en backend), `PayoutOrdersViewModel` con 4 totales agregados + filtro por status (chips), `PayoutOrderDetailsViewModel` con reservations + attachments. Descarga de comprobantes via `Launcher.Default.OpenAsync` — abre en el visor del sistema (browser/PDF viewer). Nuevo tile "💰 Payouts" en `DashboardPage` arriba de "👥 Operarios". Sin filtros de fecha en v1 — deuda MO-21b registrada para cuando haya volumen. Build limpio 0 errores. Código mobile sigue en local (MO-00 postponed). | Franklin | tourya-mobile local |
| 2026-07-15 | **MO-40 Fase D (backend hooks push) implementada — MO-40 completo**. `PushNotificationService` ampliado con 6 helpers de dominio (templates title/body + payload data `{type, targetId}` que el handler mobile interpreta). Integrado en 5 flows: (1) `PaymentService.processPayment` post-confirmación → push al turista por cada reserva + push al provider (dedup por proveedor); (2) nuevo `TourReminder24hJob` cron 8am Bogotá busca reservas `PENDING` con tourDate = mañana via nuevo `ReservationRepository.findPendingForTourDate`; (3) `CreditExpirationJob` — agregado push en 30d, 7d y expirado, en paralelo al email existente (BE-18/19); (4) `ReviewService.updateReview` — cuando el provider responde y roles isProvider, push al turista dueño de la review (traza vía reservation→cart→user). Todos best-effort — fallos de push no bloquean el flow principal. Tests unitarios 21/21 verdes (Mockito). **Estado MO-40 completo — falta validación end-to-end en device físico**. | Franklin | PR tourya-api #<pendiente> |
| 2026-07-15 | **MO-40 Fase C (mobile recepción push) + activación real del backend**. (1) Creamos nueva key JSON del service account `firebase-adminsdk-fbsvc@tourya-169d6.iam.gserviceaccount.com` vía `gcloud iam service-accounts keys create`, la subimos a Secret Manager como `tourya-firebase-admin-sdk` (versión 1) en `tourya-project-dev`, dimos `secretAccessor` al SA `tourya-dev-cloud-run`, y wireamos la env var `FIREBASE_ADMIN_SDK_JSON=tourya-firebase-admin-sdk:latest` a Cloud Run — revisión desplegada `tourya-dev-api-00134-ffz` sirviendo 100% del tráfico. Backend loguea `MO-40: Firebase Admin SDK initialized` (antes decía "NOT configured"). La key JSON local fue eliminada del disco después de subirla — solo vive en Secret Manager. (2) Fase C mobile: nuevo `IPushNotificationHandler` singleton que se suscribe a `NotificationReceived` (foreground → alert modal con "Ver"/"Cerrar") y `NotificationTapped` (bg+tap → deep-link directo). Convención de payload `data` del backend: `type` (reservation/tour/credit/review) + `targetId` opcional → routea a Shell. Handler se `Start()` en `MauiProgram` post-`Build()`. Sin channel custom (default del plugin — refinamiento futuro). Falta Fase D (hooks en flows del backend para efectivamente disparar push). | Franklin | GCP + tourya-mobile local |
| 2026-07-15 | **MO-40 Fase B (mobile registro FCM) implementada**. NuGet `Plugin.Firebase.CloudMessaging` 3.1.0 (Android only por `Condition` en el csproj). `google-services.json` descargado via Firebase CLI (creamos app nueva `com.tourya.mobile` en proyecto Firebase `tourya-169d6`, app ID `1:318643880116:android:ac284c9d61d41ad13a17e1` — coexiste con la app vieja `com.tourya.app` sin tocarla). `MainActivity.OnCreate` inicializa `CrossFirebase.Initialize(this)`. Nuevo `IDeviceTokenSyncService`: `RegisterCurrentTokenAsync` pide permiso runtime, obtiene token y hace POST al backend; `UnregisterCurrentTokenAsync` hace DELETE ANTES de limpiar el JWT en logout. `SecureStorageService` extendido con `Get/Set/RemoveFcmTokenAsync` (usa StorageKey `fcm_token`). `AuthService.LoginAsync`/`SocialAuthAsync` disparan register fire-and-forget; `LogoutAsync` invoca unregister antes de `ClearAllAsync`. Errores NO bloquean auth. Permiso `POST_NOTIFICATIONS` agregado a `AndroidManifest.xml`. **Aprendizaje operativo:** OAuth de Firebase CLI no funciona en shells sin TTY (Bash/PowerShell background, `!` prefix de Claude Code, `Start-Process` con IO redirect) — sí funciona en terminal externa real. Franklin corrió `firebase login` en una terminal Windows aparte, después yo hice todo el resto (crear app + descargar JSON + código). Código mobile sigue en local (MO-00 postponed). Fases C (recepción) y D (hooks backend) pendientes. | Franklin | tourya-mobile local |
| 2026-07-15 | **MO-40 Fase A (backend base push) implementada**. Decisión sobre push provider: **FCM (Firebase Cloud Messaging)** — rechazadas alternativas OneSignal (SaaS pago) y AWS SNS (nos ata a AWS). Reusamos proyecto Firebase `tourya-169d6` donde Franklin ya tiene acceso desde SEC-06. Piezas: migración 075 (`device_token` table con user_id + token + platform + timestamps), entidad `DeviceToken`, `DeviceTokenRepository`, `DeviceTokenService` (register upsert + unregister silencioso + findByUserId), `DeviceTokenController` con `POST/DELETE /users/device-token` (autenticados), `FirebaseConfig` (bean `FirebaseMessaging` opcional — degrada a no-op si no hay credentials, evita bloquear dev/CI), `PushNotificationService` con `sendToUser(userId, title, body, data)` y `sendToToken(...)` — ambos `@Async`. Dependencia `firebase-admin:9.4.0` en pom. Config env vars `FIREBASE_ADMIN_SDK_JSON` (contenido completo) o `FIREBASE_ADMIN_SDK_JSON_PATH` (dev local). Fase A NO integra hooks en flows existentes — eso es Fase D. Fases B (mobile registro) y C (mobile recepción) siguen. Franklin debe subir el service account JSON a Secret Manager (`tourya-firebase-admin-sdk`) para activar push en dev. | Franklin | PR tourya-api #<pendiente> |
| 2026-07-15 | **Cierre del "falso bug" de activate-account + nueva deuda FE-11 de i18n**. Luis reportó el 2026-07-14 que "el activate-account por correo no cambiaba el `RequestProvider` a `SUBMITTED`". Franklin pidió auditoría; el análisis mostró que **`AuthenticationService.activateAccount` NUNCA tocó `RequestProvider`** — nunca, ni en el commit `8f442ef` del 2026-05-31 antes de todo nuestro trabajo. El paso a `SUBMITTED` ocurre en `POST /requestProvider/user/send` (pantalla `/providers/requestproviders` con botón "Submit Request"). Luis confirmó el 2026-07-15 tras probar que efectivamente es esa pantalla y no la del link del correo. No fue regresión de nuestros cambios (SEC-04 solo agregó check de replay + `@Transactional`, sin remover lógica). Al revisar la pantalla `/providers/requestproviders`, Luis notó que está **toda en inglés** — registrado como **FE-11** (P2, S) para traducir con ngx-translate cuando toque, candidato claro para hacer con agente IA. Lección reforzada: cuando se reporta bug operativo cruzando múltiples componentes, auditar el código completo antes de asumir regresión. | Franklin + Luis | Doc 17 FE-11 |
| 2026-07-15 | **MO-12 UI créditos + transferencia mobile cerrada**. Nuevo `CreditsPage` con saldo agregado, tres secciones (Activos / Reservados / Historial) y botón "Transferir a otro turista" en cada crédito activo. Nuevo `TransferCreditPage` con lookup por documento → dialog confirm → `POST /credits/{id}/transfer`. Ampliado `CreditService` con `LookupTouristByDocumentAsync` + `TransferCreditAsync` (backend ya expone `GET /credits/tourist-lookup` y `POST /credits/{id}/transfer`, no se tocó). Modelo `ClientCredit` extendido con `transferredFromUserId`/`transferredAt` + helpers `AvailableAmount` y `WasTransferred`. Nuevo tile "💳 Mis Créditos" en `ProfilePage`. **Primera implementación de UI de créditos en cualquier cliente Tourya** — el web nunca la tuvo (solo el service). Errores mapeados: 400 → "el crédito no puede transferirse", 404 lookup → "no se encontró turista". Confirm dialog explícito sobre irreversibilidad. Código mobile sigue en local. | Franklin | tourya-mobile local |
| 2026-07-15 | **MO-22 + MO-23 + MO-24 operarios mobile cerrados en bloque**. Luis pidió explícitamente que el provider pueda gestionar sus operarios desde el mobile. Backend ya exponía 5 endpoints `/provider/users*` (list, create, update, principal-tour, reset-password). Implementadas en mobile: `ProviderOperatorService` (cliente), `OperatorsPage` (lista + FAB + ActionSheet), `OperatorFormPage` (crear/editar unificado con multi-select tours + picker tour principal), `ResetOperatorPasswordPage`. MO-23 resuelto vía `Share.Default.RequestAsync` (share sheet nativo — más flexible que deep-link directo a WhatsApp, permite al usuario elegir cualquier app). Nuevo tile "👥 Operarios" en el `DashboardPage` del provider en vez de sumar 6to tab (los 5 tabs actuales ya están al límite). Sin cambios en backend. Código mobile sigue en local (MO-00 postponed). | Franklin | tourya-mobile local |
| 2026-07-15 | **MO-11 Wishlist mobile cerrada**. Nueva `WishlistPage` en el TabBar del tourist con lista paginada (pull-to-refresh + infinite scroll + empty state), botón ❤️/🤍 en `TourDetailPage` con toggle optimista, servicio `WishlistService` cliente de los 4 endpoints del backend (GET/POST/DELETE /wishlist + POST /wishlist/search). Backend ya existía completo, no se tocó. Ajuste transversal: nuevo overload `ApiService.DeleteAsync<T>(endpoint, body)` porque el endpoint DELETE /wishlist requiere body JSON y HttpClient no lo soporta nativo. Auth: los 4 endpoints requieren JWT; el `AuthHandler` ya adjunta el Bearer automáticamente. Si el user no está logueado y toca el ❤️, muestra alert "Inicia sesión". El código mobile sigue sin repo Git (MO-00 postponed) — riesgo abierto. Falta validación en device físico. | Franklin | tourya-mobile local |
| 2026-07-14 | **HTTPS en LB dev + CORS whitelisting de `dev.tourya.co`**. Luis agregó el DNS `dev.tourya.co → 34.160.22.16` (subdominio) — Franklin cerró la deuda de "LB dev no tiene forwarding rule HTTPS" abierta el 2026-07-10 aprovisionando cert Google-managed (`tourya-dev-cert`) + target HTTPS proxy + forwarding rule 443 en la misma IP del LB, reusando el `tourya-dev-url-map` existente (mismo routing `/api/*` → api, `/*` → front). Cert emitido por Google Trust Services WR3, ACTIVE en ~10 min, auto-renovación. Descubrimiento inmediato al probar login: backend rechazaba con 403 `Invalid CORS request` porque `BeansConfig.allowedOrigins` no incluía `https://dev.tourya.co`. Fix: agregar `https://dev.tourya.co` + `http://34.160.22.16` (fallback si alguien entra por IP) y retirar mención genérica a EC2 del CLAUDE.md. Franklin ya agregó el nuevo origen en Google OAuth Client `318643880116`. Facebook App queda pendiente porque el owner es Luis (bloqueante para social por Facebook, no para login normal ni Google). | Franklin | PR tourya-api #<pendiente> |
| 2026-07-13 | **SMTP restaurado + auditoría MO-01/MO-20 en mobile**. (1) Luis regeneró app-password de `luis.mendoza@tourya.co` (verificó 2FA activo desde 10-jul, password rotado 11-jul). Franklin aplicó v3 del secret `tourya-smtp-password` + redeploy → revisión `tourya-dev-api-00125-jcg`. Test end-to-end: POST /auth/register con `franklinmarcano1970+t07130717@hotmail.com` (plus-addressing de Hotmail) → HTTP 202, correo llegó a spam (esperable hasta SPF/DKIM/DMARC bien alineados). BE-18/BE-19 empieza a mandar recordatorios en la próxima corrida 5am Bogotá. Deuda descubierta: `_user.email` es VARCHAR(50) — muy corto para emails largos (RFC 5321 estándar = 254). Luis agregó DNS SPF/DKIM/DMARC pero SPF con typo (`_://google.com` en vez de `_spf.google.com`) — pendiente corrección + mover A record `@ → 34.160.22.16` a `dev.tourya.co`. (2) **MO-01** cerrado: `Constants.cs:5` cambió de `http://44.203.38.85:8088/api/v1/` (IP EC2 AWS legacy apagada) a `http://34.160.22.16/api/v1/` (LB GCP, coherente con el patrón "todo por el LB" del frontend web). (3) **MO-20** cerrado por auditoría — el 90% ya estaba implementado (UI + ViewModel + servicio) pero tenía un bug latente: PATCH con `application/json` cuando el backend espera `multipart/form-data` para `/public/save/review/{reviewId}`. Habría fallado con 415 al primer uso real. Fix: nuevo `PatchMultipartAsync` en `ApiService` + `ReplyToReviewAsync` refactorizado armando el multipart con part `reviewData`. Sin fotos porque UI actual es solo texto. **Nota crítica pendiente**: el código MAUI sigue sin repo Git (`tourya-mobile` solo local) — riesgo alto de pérdida cada vez que se modifica. | Franklin | Local mobile + Cloud Run reconfigured |
| 2026-07-12 | **BE-18/BE-19 notificaciones de expiración de créditos (RN-036)**: nuevo `CreditExpirationJob` cron 5am Bogotá que envía 3 tipos de correo: recordatorio 30 días antes, recordatorio 7 días antes, y aviso al expirar. Idempotencia por timestamps (`reminder_30d_sent_at`, `reminder_7d_sent_at`, `expired_notified_at`) — si SMTP falla, se reintenta al día siguiente. Nuevo valor `EXPIRED` en `CreditStatusEnum` — todos los checks existentes `status != CREATED` en el resto del código lo rechazan automáticamente, cero regresión. Migración 074 hace backfill de créditos ya vencidos a EXPIRED sin reenviar correo retroactivo. 7 tests JUnit (Mockito). **Caveat operativo:** los correos dependen del SMTP que está caído desde ayer (`535 BadCredentials` de Google contra `luis.mendoza@tourya.co`) — el código está listo y el job seguirá reintentando; cuando Luis regenere el app-password los correos empiezan a fluir sin cambios adicionales. | Franklin | PR tourya-api #173 |
| 2026-07-11 | **BE-20 auditoría reschedule + primera suite JUnit real del proyecto**: los 3 casos de RN-033 (EQUAL/LOWER/HIGHER) ya estaban implementados sólidamente en `handleRescheduleEqualOrLower` y `handleRescheduleHigher` (usan `CREDIT_EXPIRATION_MONTHS` de `app_config` para RN-036, marcan `RESCHEDULED`, recalculan availability, todo `@Transactional`). Fixes menores: guard `newDate >= today` en `rescheduleReservation` (antes permitía reagendar al pasado si existía TourSchedule) + fix cosmético del comentario "5." → "4." en `handleRescheduleHigher`. Se agregó **la primera suite real de unit tests** del proyecto: 14 tests con Mockito en `ReservationServiceRescheduleTest` — 9 para `validateRescheduleReservation` (happy path + 8 razones de rechazo) y 5 para guards de `rescheduleReservation`. Deuda registrada como BE-20b (no bloqueante): test integrado con `@SpringBootTest` + Testcontainers para verificar los 3 flows end-to-end con BD real. | Franklin | PR tourya-api #170 |
| 2026-07-10 | **FE-05 + endpoint PATCH `/tours/admin/{id}/porcentajeTourya`**: cierra RN-015 end-to-end. Backend (PR tourya-api #169) habilita el DTO `UpdatePorcentajeTouryaRequest` que existía como dead code desde BE-01/02 (PR #163); nuevo endpoint solo ADMIN/BACKOFFICE con `Utils.isTouryaBackoffice` y validaciones `DecimalMin(0.0)`/`DecimalMax(1.0)`. Frontend (PR tourya-front #63) agrega input numérico (%) al modal de aprobación de `tour-admin-detail`, pre-poblado con el valor actual del tour (o 15% default); si cambia dispara PATCH + PUT accept, si no cambia solo accept. `TourAdminDto` incluye ahora el campo opcional `porcentajeTourya`. Revisiones desplegadas: `tourya-dev-api-00114` + `tourya-dev-front-00030` (aprox). Edición post-aprobación queda como FE-05b futuro. | Franklin | PRs tourya-api #169 + tourya-front #63 |
| 2026-07-10 | **Auditoría RN-021: BE-03 + BE-04 + FE-07 ya estaban hechos** — al ir a arrancar el "combo RN-021" (eliminar `isUnlimitedCapacity` de `tour_schedule`) se descubrió que el trabajo ya se había hecho hace 3 meses: migración **029** del 2026-04-08 dropeó las columnas de `tour_schedule` y `tour_schedule_config`. El backlog quedó desactualizado. Verificado empíricamente: (a) BD dev no tiene la columna; (b) todos los services del backend y el frontend leen de `tour.isUnlimitedCapacity` (correcto — fuente de verdad); (c) `TourSchedule.java` no tiene la propiedad; (d) `tour-schedule.component.ts` tiene un getter que expone `tour?.isUnlimitedCapacity` para readonly, ya no hay input. Backlog + docs 05, 04, 08 sincronizados con el estado real. Deuda residual (no bloqueante): SP `sp_get_tour_schedule` (viejo, sin `_json`) todavía referencia `ts.is_unlimited_capacity`; nadie lo llama (grep 0), es dead SQL — un `DROP FUNCTION` cierra la deuda cuando se quiera. | Franklin | — (docs directo a develop) |
| 2026-07-10 | **Fix `hasRole` vs `hasAuthority` en AppConfigController**: al probar el panel FE-04 con user id 49 (rol ADMIN vía UPDATE directo en BD + relogin), `PUT /config/{key}` devolvía 500 con `AuthorizationDeniedException` aunque el JWT trajera `authorities: ["USER","ADMIN"]`. Causa: en Spring Security `@PreAuthorize("hasRole('X')")` internamente compara contra `ROLE_X` (agrega el prefijo automáticamente), pero `User.getAuthorities()` de este proyecto devuelve `SimpleGrantedAuthority(role.getName())` **sin** el prefijo `ROLE_`. Fix quirúrgico: cambiar el `@PreAuthorize` a `hasAuthority('ADMIN')` (comparación literal). Endpoint solo tuvo 1 usuario en la vida (Franklin al probar hoy) — bug nace con BE-09 (PR #160, 2026-07-09) y muere con este fix. Deuda futura: si algún día se decide alinearse con la convención Spring, hay que auditar todos los `.getAuthority()` del código y agregar prefijo en `User.getAuthorities()` en bloque. | Franklin | PR tourya-api #168 |
| 2026-07-10 | **Postmortem `apiUrl` del frontend — 2 commits, la sesión corrigió mal y luego corrigió bien**: (a) Se detectó Mixed Content al abrir el login por `https://tourya-dev-front-...run.app` (URL directa Cloud Run del front, HTTPS) porque `environment.ts` apuntaba a `http://34.160.22.16/api/v1`. Ese IP es el **LB HTTP dev de GCP** (`tourya-dev-lb-ip`, no AWS como se pensó inicialmente), reservada en el proyecto para el forwarding rule `tourya-dev-http-rule` que enruta `/*` → Cloud Run front y `/api/*` → Cloud Run api. (b) Fix incorrecto en PR #61: `apiUrl` a URL absoluta HTTPS del Cloud Run backend. Convertía same-origin → cross-origin y disparaba CORS al acceder por el LB (que es el flujo real). El origin `http://34.160.22.16` no está en `allowedOrigins` del backend (nunca estuvo; en same-origin no importa). (c) Fix correcto en PR #62: `apiUrl` a URL relativa `/api/v1`. **La forma correcta de acceder al frontend dev es por `http://34.160.22.16` o `http://tourya.co` (el LB), NO por la URL directa de Cloud Run del front.** Origen del bug original: commit `a8d599c` del 2026-05-03 por devcristianamador — cambio colateral en un PR de "scheduleChanges" que rompió el patrón de URL relativa (Franklin lo había establecido en `edd29f4` del 2026-04-22). Latente porque el frontend seguía en EC2 hasta el 2026-07-09; nadie probó login post-migración. Deudas registradas: (1) LB dev no tiene forwarding rule HTTPS (solo :80), (2) `angular.json` no tiene `fileReplacements` configurados. | Franklin | PR tourya-front #61 (revertido por #62) + #62 |
| 2026-07-10 | **Migración SMTP backend a Workspace Tourya (dev)**: Luis creó SMTP Relay "Tourya API SMTP Relay" en el Workspace `tourya.co` y compartió app-password de `luis.mendoza@tourya.co`. Test empírico local + desde Cloud Run confirmó: (a) auth OK, (b) sin restricción de IP en el relay, (c) mails llegan pero caen en spam porque **SPF/DKIM/DMARC NO están configurados** en GoDaddy. Aplicado en `tourya-project-dev`: secret `tourya-smtp-password` v2 con nuevo password (v1=WASS conservada para rollback), Cloud Run env var `MAIL_USERNAME=luis.mendoza@tourya.co`. Revisión activa: `tourya-dev-api-00107-8j6`. Prod (`tourya-project-493820`) sigue en Workspace de WASS — pendiente hasta que Luis termine SPF/DKIM y estemos estables varios días en dev. Cleanup de código (OpenApiConfig + AuthenticationService.sendEmailTest) va en PR aparte. | Franklin + Luis | Cloud Run + PR tourya-api pendiente |
| 2026-08-11 | **Timezone del backend en America/Bogota** (TC-020 #235 bug c). Cloud Run corría el container en UTC → `LocalDateTime.now()` "bare" (~28 sitios) resolvía a UTC (offset 5h). Fix: `Dockerfile` instala `tzdata` + `ENV TZ=America/Bogota` + `-Duser.timezone=America/Bogota` en el `ENTRYPOINT` + bean `DateTimeProvider` en `JpaAuditingConfig`. Jobs cron y auditoría ahora reflejan hora Colombia. Nueva RN-060. | Franklin | PR #244, doc 05 §RN-060 |
| 2026-08-11 | **Reportes DIMAR con flag RED son inmutables** (TC-018 #227, 5 iteraciones). El hook `@Async @TransactionalEventListener(AFTER_COMMIT)` **nunca se ejecutó** en producción; el fix definitivo es **direct call** desde el service. Regla operativa: cuando el flujo también es async, los eventos AFTER_COMMIT+@Async son frágiles. Nueva RN-057. | Franklin | PR #239, doc 05 §RN-057 |
| 2026-08-10 | **PROVIDER no ve datos del cliente hasta 1 día antes del tour** (TC-021 #236). Scrub de payerName/email/phone/documentNumber + serviceResponsible en response del PROVIDER cuando `reservationDate - today > 1`. ADMIN y BACKOFFICE_OPERATION siempre ven todo. Nueva RN-058. | Luis + Franklin | PR #241, doc 05 §RN-058 |
| 2026-08-11 | **Tours siempre se guardan con nombre/descripción en `es`** (TC-017 #220 opción B). Aún si el PROVIDER usa la UI en `en/pt`, el frontend copia el texto al slot `es` antes de mandar al backend. Consistente con el constraint DB `tour_name_es_required`. Nueva RN-059. | Luis + Franklin | tourya-front PR #107, doc 05 §RN-059 |
| 2026-08-12 | **Reagendar ajusta booking count del día original y del nuevo** (TC-019 #231 bug 3). Migración 089: `fn_slot_booked_units_on_schedule` incluye `RESCHEDULED` en el filtro de reservas activas + backfill idempotente de 792 filas de `slot.bookings` y 404 de `slot.availability`. `ReservationRepository.countActiveBookingUnitsForSlotOnDate` alinea el guard Java (`ensureSlotHasCapacity`) con el helper SQL. **Regla nueva RN-061**: reservas en `RESCHEDULED` cuentan como activas para disponibilidad en la fecha destino. | Franklin | PR #246, doc 05 §RN-061 |
| 2026-08-12 | **`slotPorcentajeTourya` editable desde config-slot base para BACKOFFICE** (TC-019 #231 bug 4). `TourScheduleConfigSlotDto` expone el campo (0-100, nullable) + `manageSlotsUpdate` lo honra cuando `Utils.isTouryaBackoffice(roles)` y recalcula `price = providerPrice × (1 + slotPct)` via `applyPriceDtoToEntity`. Idempotente. Migración 090: backfill de drift acumulado (4 filas dev). Endpoint per-schedule `PUT /percentage/{slotId}` no se tocó. Refinamiento de RN-015 (no requiere nueva RN). | Franklin | PR #247, doc 05 §RN-015 + doc 08 §Fase 10 |
| 2026-08-12 | **Header duplicado en `maritime-activity-reports` retirado** (TC-008 #195). El `<h2>Maritime Activity Reports</h2>` hardcodeado dejaba de tener sentido cuando el shell admin renderizaba su breadcrumb. Cierre real de TC-008 tras las 2 pasadas de sidebar (PRs #96, #109). | Franklin | tourya-front PR #110 |
| 2026-08-14 | **Sprint 3 A/B mobile + Sprint 4a autoVerify + WompiHelper cleanup**. Sprint 3 A (`TourDraftService` autosave en `AppDataDirectory/tour_draft.json` + `TourGalleryUploadService` upload background con `SemaphoreSlim(3)` + progress per-item + retry, `TourFormPage` Step 7 Galería) cierra MO-30 y MO-32. Sprint 3 B (`ScheduleCalendarPage` con `SfCalendar` toggle Día/Semana/Mes + special dates + botón "💰 Copiar precios de otro tour" en `ScheduleTemplateFormPage` reusando endpoint TC-019 `GET /tour-schedules/templates?tourId={id}` via nuevo `ScheduleService.GetTemplatesForTourAsync`) cierra MO-33 y completa el alcance original de MO-34. Sprint 4a (`MainActivity.cs` con `AutoVerify=true` en el `IntentFilter` del deep-link + `assetlinks.json` publicado en `tourya-front` PR #117) cierra MO-43b. **WompiHelper cleanup**: reemplazado `EscapeJs` manual (solo `'`+newlines) por `System.Text.Json.JsonSerializer.Serialize` para XSS/escape completo del user-input al HTML/JS de `BuildCheckoutHtml`; `redirectUrl` ahora usa `Constants.WebBaseUrl` (antes hardcode a prod) — `CheckoutViewModel.cs:176`. Docs 00, 15, 17 sincronizados | Franklin | tourya-mobile local + tourya-front PR #117 |
| 2026-08-14 | **Sprint 5a responder reseñas provider + auditoría Sprint 5b + 2 deudas nuevas MO-53/MO-54**. Sprint 5a: nueva `ProviderReviewsViewModel` + Page con flujo completo de responder reseñas (TextEditor + Enviar/Cancelar + nuevo `StringNotEmptyToBoolConverter` + 7 keys i18n es/en/pt) reusando `ReviewService.ReplyToReviewAsync` (existía desde MO-20, sin cambios backend). Cierra el ❌ del doc 14 "Responder reseñas provider" → ahora ✅. Sprint 5b auditó 3 ítems ⚠️ del doc 15: (1) Reagendar reserva turista 3 casuísticas → ❌ NO implementado (registrado como **MO-53**, ~4-6h, P1); (2) Confirmar reserva manualmente fallback QR → ✅ YA implementado en `QrScannerPage.xaml:29-34` + `QrScannerViewModel.cs:164-183` (doc 15 decía "En ProviderReservationsPage" — ubicación incorrecta, se corrige); (3) Login `mustChangePassword` operario → ❌ NO implementado (**security issue moderado**: operario usa clave temporal sin forzar cambio; registrado como **MO-54**, ~3-4h, P1). Docs 00, 15, 17 sincronizados | Franklin | tourya-mobile local |
| 2026-08-14 | **Sprint 6 mobile — MO-53 + MO-54 cerradas + WompiHelper cleanup**. Cierre en bloque de las 2 deudas Sprint 5b + hardening del checkout. **Sprint 6a MO-54** (commit `8f50f1b` + merge `8693c97`): cierra security issue moderado del operario. Backend ya expone `AuthenticationResponse.mustChangePassword` + `PATCH /users` self-service (`UserController.java:30-38`); cero cambios backend, primer consumo desde mobile. Piezas nuevas: guard en `LoginViewModel` que navega a `//change-password` cuando el flag viene true, `ChangePasswordPage` + VM + DTO + `PatchAsync<TRequest>` en `ApiService`, ruta + DI, 11 keys i18n. **Sprint 6b MO-53** (commit `58d7a4f` + merge `1af997a`): cierra reagendar turista 3 casuísticas (RN-033). Backend ya expone `PUT /reservations/{id}/reschedule` (ReservationController:387); cero cambios backend, primer consumo desde mobile. **Sorpresa del contrato**: para HIGHER el backend NO abre Wompi directo por el delta — cancela reserva anterior, abre crédito por el valor pagado, agrega el nuevo tour al carrito (response `priceComparison=HIGHER` + `status=CANCELLED_AND_ADDED_TO_CART`); el turista completa el pago via checkout normal del carrito. **Mismo patrón que web**, intencional del backend. Piezas mobile: `RescheduleReservationAsync` + DTOs, botón "Reagendar" en `ReservationDetailPage` (visible sólo si `canReschedule==true`), `RescheduleReservationPage` con dispatch por casuística (EQUAL/LOWER → alert + volver Mis Viajes; HIGHER → alert + navigate `//tourist/cart`), 12 keys i18n + `common.ok`. **WompiHelper cleanup** (commit `2cecbb2` + merge): cierre técnico del hardening abierto por Sprint 4a — `EscapeJs` manual (solo `'`+newlines) → `JsonSerializer.Serialize` (cubre XSS/escape completo `<`, `\`, `</script>`); `redirectUrl` hardcode prod → `Constants.WebBaseUrl` (dev-aware). Docs 00, 15, 17 sincronizados. Docs 05 y 09 NO se tocan (sin RN nueva; los endpoints ya existían en backend) | Franklin | tourya-mobile local |
| 2026-08-14 | **Sprint 7 mobile — offline extendido operario (MO-56)**. Cierre del ítem "Modo offline extendido" del Ciclo 5 del roadmap doc 15. Extiende `IReservationCacheService` (que MO-50 solo aplicaba al Dashboard) para cachear también la lista paginada del `ProviderReservationsPage` — el operario en el bote/muelle sin señal ahora puede seguir viendo TODAS las reservas que ya cargó, no solo las del día del Dashboard. Nuevo path `FileSystem.CacheDirectory/provider_reservations_page.json` (separado del `reservations_today.json` de MO-50 para no colisionar). Cache save-on-success en cada `LoadMore` exitoso; fallback-on-error activa `IsOfflineMode=true` + banner amarillo reusando estilo Dashboard MO-50; `_hasMorePages=false` en el fallback evita paginar contra backend caído (pull-to-refresh reinicia el flow). 2 keys i18n `reservation.offline.banner{.recent}` es/en/pt. Merge local commit `34d32ae` → merge `29dd8d6`. **Cero cambios backend**. **Item 2 del Sprint 7 (sub-grouping por franja horaria mañana/mediodía/tarde) NO se implementó** — el ViewModel ya ordena `ThenBy(SlotTimeStart)` dentro del día pero sin sub-grupos por franja; decisión pendiente Franklin si vale una iteración corta MO-56b. Deudas descubiertas: (a) baseline warnings mobile es 140, no 139 como decía la memoria; (b) 2 warnings MAUIG2045 pre-existentes en `ProviderReservationsPage.xaml` por `LoadReservationsCommand`/`LoadMoreCommand` (source generator no las ve — no introducidas por este sprint). Docs 00, 15, 17 sincronizados | Franklin | tourya-mobile local |
| 2026-08-14 | **IA-07 Operator Support backend cerrado — segundo agente IA productivo del bloque**. Cierra el ítem IA-07 del backlog (doc 17): asistente al operador para el wizard de tour + gestión de reseñas. 4 endpoints action-specific (no chat unificado) bajo `/agents/operator-support/*`: `POST /suggest-tour-content` (nombres SEO + descripción 200-400 palabras en español + tags del catálogo `tags`), `POST /price-alert/{tourId}` (severity OK/WARN/CRITICAL/UNKNOWN + rango min/max/median de tours comparables por subcategoría — heurística fallback +/-15%/±35% si el LLM devuelve JSON inválido, RN-014 solo alerta), `POST /draft-review-reply/{reviewId}` (borrador + tone PROFESSIONAL/WARM/APOLOGETIC + detectedLocale, el operador aprueba antes de `PATCH /public/save/review/{reviewId}`), `POST /validate-gallery` (cero costo LLM — reusa reglas de `GalleryValidator` sobre metadata pre-upload). Servicio `OperatorSupportService` con 4 métodos públicos, guardrails idénticos a IA-02 (deny-list `WOMPI_INTEGRITY_SECRET`/`JWT_SECRET`/etc → escala humano sin llamar al LLM + audit `rejected`; scrub recursivo de `providerPrice`/`slotPercentageTourya`/`slotPorcentajeTourya`/`porcentajeTourya` de todo JSON al LLM; budget guard `OperatorSupport` con cap default $20/mes ya en `ConfigKeyEnum.AGENT_BUDGET_OPERATORSUPPORT_USD_MONTHLY`). Autorización owner-based: `requireTourOwnership` valida `tour.provider.id == providerService.findByUser(auth).id` antes de cualquier call al LLM — para `draft-review-reply` se valida a través de `review.tourId`. **Modelo Gemini 2.5 Pro** (razonamiento + generación de JSON estructurado) — cambio vs Sonnet 5 del brief por mismas razones que IA-02 (ADC en Cloud Run, ~40% más barato, framework provider-agnostic). Costo esperado con Gemini: **$1-2/mes** (baja vs $2-4 estimado con Sonnet). Nuevo `OperatorSupportRepository` con 2 native queries: `findAvgAdultPriceByTourId` + `findComparableAdultPricesBySubcategory` (limit 20 defensivo, `CAST(:subCategory AS tour_subcategory_enum)` igual que `TourRepository.findAlternativesBySubCategoryQuery`) — usa `price` público de `tour_schedule_config_price` para no leakear `provider_price` interno entre operadores. **Traducción es→en/pt DEFERRED a IA-09** (Google Cloud Translation): TODO explícito en el service, comentario en el prompt `suggest-tour-content.v1.txt` — cuando IA-09 exista es 1 wire-up. 3 prompts versionados en `resources/prompts/operator-support/*.v1.txt`. 11 unit tests (`OperatorSupportServiceTest`) con `MockLlmClient` cubren happy paths + JSON malformado + auth error + guardrails + heurística de pricing sin LLM. **Cero cambios en tabla `agent_audit_log`** (reusa migración 092 de IA-02 con `metadata JSONB`). **Cero cambios en `TravelConciergeService` / `AnthropicClient`**. Feature flag `agents.operator.enabled=${AGENTS_OPERATOR_ENABLED:true}` para apagar rápido sin re-deploy. Build verde (`./mvnw clean package -DskipTests`), tests verdes (11/11 `OperatorSupportServiceTest`, 33/33 el paquete IA). Docs 00, 09, 16, 17 sincronizados. **Nota**: docs 05 y 12 no se tocan (sin RN nueva, guardrails idénticos a IA-02 ya documentados). **Autorización 401**: consistente con `InsufficientPrivilegesException` global del proyecto (que mapea a 401 en `GlobalExceptionHandler`, no a 403 — inconsistencia del codebase que se mantiene por consistencia; deuda para futuro cleanup si el equipo decide alinear a HTTP semántico) | Franklin | rama `feature/ia-07-operator-support-backend` |
| 2026-08-14 | **Sprint 8 completo end-to-end — Travel Concierge IA activo en backend + web + mobile**. **Primer agente IA productivo de Tourya.** 3 frentes cerrados en el mismo día: (1) **IA-02 backend** (PR #263 mergeado) — `TravelConciergeService` con function calling loop max 5 iter, endpoint `POST /agents/travel-concierge/chat`, cambio de provider a Vertex AI Gemini 2.5 Pro (razones: reusa ADC GCP, ~40% más barato, `ILlmClient` ya provider-agnostic desde IA-01), `GeminiClient` + `LlmClientConfig` con selector `agents.provider`, guardrails en código (deny-list secretos → escala humano; scrub `providerPrice`/`slotPercentageTourya`; contador fraude por sessionId con umbral 3), migración 092 (`agent_audit_log.metadata JSONB` + GIN index) **aplicada a Cloud SQL dev antes del merge**, 9 tests con `MockLlmClient`. (2) **FE-Concierge Angular** (PR tourya-front #118 mergeado) — `ConciergeChatWidget` FAB flotante bottom-right expandible en `list-tours`/`tours-detail`/`cart-summary`, session `sessionStorage`, chips por acción, banner escalado. (3) **MO-Concierge MAUI** (commit local `7c76f39` + merge `190f1ba`) — `ConciergeChatPage` modal + FAB en `TourDetailPage`/`CartPage`/`ExplorePage`, session `SecureStorage`, rotación en logout `AuthService.LogoutAsync` (defense-in-depth doble llamada), 2 comandos como `AsyncRelayCommand` propiedades explícitas (blueprint para cleanup MAUIG2045). **Infra GCP habilitada por Franklin**: Vertex AI API + rol `roles/aiplatform.user` a la SA Cloud Run + región `us-central1`. **Cambio de decisión operativa**: agente Claude aplica migraciones a Cloud SQL dev directamente vía `gcloud.cmd` + docker+psql (Franklin insistió: *"tienes acceso a la consola y todo lo corrias vos no me pongas a mi hacer nada"*), y aplicar migración SIEMPRE antes del merge del PR que la contiene. **Cierre estado MVP mobile doc 14**: turista cerró el último ⚠️ parcial ("Buscar tours con Travel Concierge (IA)") — ahora 100% del alcance del doc 14 cerrado. Costo estimado Travel Concierge con Gemini: **$8-12/mes** vs $20-25 con Anthropic. Docs 00, 15, 17 sincronizados. Docs 05 y 12 NO se tocan (sin RN nueva; guardrails ya documentados en 16) | Franklin + Luis | tourya-api PR #263 + tourya-front PR #118 + tourya-mobile local |
| 2026-08-15 | **Sprint 9 completo end-to-end — Operator Support IA activo en backend + web + mobile**. **Segundo agente IA productivo de Tourya.** A diferencia de IA-02 (chat conversacional), IA-07 se diseñó como **4 endpoints action-specific** — el provider tiene casos de uso bien delimitados y los touchpoints se integran como botones `✨` aditivos en flujos existentes (**cero regresión**). 3 frentes cerrados en 2 días: (1) **IA-07 backend** (PR #265 mergeado 14-ago) — `OperatorSupportService` con 4 endpoints (`suggest-tour-content`, `price-alert/{tourId}`, `draft-review-reply/{reviewId}`, `validate-gallery`), guardrails idénticos a IA-02 + autorización owner-based (`requireTourOwnership`), modelo Gemini 2.5 Pro (~$1-2/mes), cero migración BD (reusa `agent_audit_log.metadata` de IA-02), 11 tests con `MockLlmClient`. (2) **FE Operator Support Angular** (PR tourya-front #119 mergeado hoy) — 4 touchpoints en `add-tour` (botón sugerir + modal), `tour-schedule` (badge pricing on-blur), `tour-gallery` (pre-upload validate), `provider-reviews` (borrador respuesta reseña). 20 keys i18n. Build production verde 0 warnings nuevos. (3) **MO Operator Support MAUI** (commit local `b6de801` + merge `cc45149` hoy) — mismos 4 touchpoints. **Descubrimiento clave**: en mobile los precios del tour viven en `ScheduleTemplateForm` no en `TourFormPage` — el agente adaptó el brief y puso el price-alert donde tiene sentido. Nueva `OperatorSuggestModalPage`. Patrón `AsyncRelayCommand` propiedad explícita conservado (Sprint 8 blueprint). Baseline 140 warnings intacto. **Nota flow MO**: el agente stopped por corte de proceso hizo el trabajo (14 archivos, 803 líneas) directo sobre `develop` del checkout compartido; recovery manual con build check + rama + commit + merge no-ff + cleanup. **Provider ahora con asistencia IA en el flujo entero** (creación tour + pricing + galería + reseñas). Deudas menores no bloqueantes: alineación tipos `subcategoryId: number` (FE + MO), `reviewId: number` MO, `InsufficientPrivilegesException` 401 vs 403 backend. Costo esperado Operator Support: **$1-2/mes** vs $2-4 con Anthropic. Docs 00, 15, 17 sincronizados. Docs 05, 12, 14, 16 NO se tocan (sin RN nueva, guardrails ya documentados, IA-07 es refinamiento de features existentes, doc 16 ya se actualizó en v2.90) | Franklin | tourya-api PR #265 + tourya-front PR #119 + tourya-mobile local |
| 2026-08-13 | **Sprint 2 mobile cerrado** — 9 TCs del ciclo QA agosto portados al MAUI Android (TC-007/009/014/016/017 display/018/019 multi-ageType/020/021). Mobile alineado 1:1 con web al 13-ago para lo que aplica al mobile. **Decisiones de scope aprobadas por Luis**: (P6) `addressType=HOTEL_PICKUP` en creación desde `TourFormPage` mobile **SÍ** — deuda para futuro ciclo; (P1) guest checkout mobile TC-012/013 **NO** — mobile obliga cuenta, Ciclo 7 del roadmap cancelado. **Decisiones Franklin**: iOS confirmado fuera de scope (P5); Syncfusion license Touya Marketplace S.A.S. registrada (P4 = MO-02); URL API migrada a HTTPS `dev.tourya.co` (MO-01b). Repo Git remoto (P3 = MO-00) sigue diferido. | Luis + Franklin | doc 15 §Cierre Sprint 2 mobile |
| 2026-08-13 | **Devolución en efectivo de crédito habilitada (TC-022 #253)** — flujo 2 pasos: turista solicita (`CREATED → REFUND_REQUESTED`), ADMIN/BACKOFFICE completa subiendo comprobante (`REFUND_REQUESTED → REFUNDED`). Comprobante en storage abstraction (`IStorageService` — S3 o GCS según env) al path `credit-refund-proofs/{creditId}/...`. Reembolsos manuales previos quedan fuera del sistema — sin backfill. No reversible por diseño. Migración 091 aplicada a Cloud SQL dev + bonus fix: CHECK constraint `credit_status_check` normalizado con catálogo completo (cierra deuda mig 074 que empezó a usar `'EXPIRED'` sin actualizar el CHECK). **Nueva RN-062**. Cerrado en los 3 frentes en el mismo día. | Luis + Franklin | PR #254 backend + PR front #115 + commit mobile local `2d770f8`, doc 05 §RN-062 |
| 2026-08-12 | **Mensaje Hotel Pickup extendido a 4 vistas del turista** (TC-017 #220). `tours-detail`, `tour-booking-confirmation`, `list-tours` (grid+list) y `cart-summary` muestran ahora el aviso largo/corto de Hotel Pickup vía i18n `tour.hotelPickup.{longMessage,shortMessage}` (ES/EN/PT). Bonus: removidos fallbacks fake en `cart.service.ts` (Cartagena/Bolívar/Colombia/Plaza de la Aduana) que enmascaraban tours Hotel Pickup — bug preexistente. | Franklin | tourya-front PR #111 |
| 2026-08-15 | **IA-09 Traducción automática es→en/pt-BR con Google Cloud Translation cerrada — tercera integración productiva del bloque doc 16 tras IA-02 y IA-07**. Cierra el ítem IA-09 del backlog (doc 17) y desbloquea el TODO deferred que IA-07 dejó en `OperatorSupportService`. Nuevo paquete `com.tourya.api.services.translation` con `ITranslationService` + `GoogleCloudTranslationService` (v3, auth ADC, SA `tourya-dev-cloud-run` con `roles/cloudtranslate.user`, API `translate.googleapis.com` habilitada en `tourya-project-dev` el mismo día). Contrato "nunca throws": si el service está desactivado o el call falla devuelve `null` y `TranslatedField#get` hace auto-fallback a español — el tour nunca se rompe por la traducción. Modo no-op degradado si `agents.translation.enabled=false` o el SDK falla al inicializar. Hook en `TourService.saveCreateOrUpdateFullData`: publica `TourTranslationEvent(tourId)` → `TourTranslationEventListener` (`@TransactionalEventListener(AFTER_COMMIT)`) → `TourTranslationApplier.translateTourAsync` (`@Async @Transactional`) — mismo patrón que `CreditRefundEventListener` (v2.83) y `MO-40b` push hooks. El save del tour NO espera al call de Google Translate — el tour se persiste en español y en background se rellenan en/pt-BR. El applier recorre todos los campos JSONB del tour: `tour.name`, `tour.description`, `TourAddress.location`, `TourMainAttraction.description`, `TourIncludesExcludes.description` (cubre includes y excludes), `TourFaq.question`+`answer`, `TourItinerary.title`+`description`, `TourCancellationPolicy.observations`, `TourGallery.description`. **Guardrail no sobrescribir (RN-011)**: para cada campo solo se rellena en/pt si vienen null o blank — cualquier valor que el provider haya escrito manualmente (o aceptado de una sugerencia IA-07) queda intacto. **Convención de códigos**: el JSONB usa `pt` como clave (mismo que el resto del proyecto — grep `"pt":` en migración 056 lo confirma); internamente el service mapea `pt`→`pt-BR` para el call a Google (portugués de Brasil, decisión de Luis en `traduccion-automatica-tours.md`). El `en` se pasa como `en` (Google usa neutro por defecto). **Wire-up del TODO IA-09 en `OperatorSupportService`**: el comentario `// TODO IA-09` se cerró — el agente sigue devolviendo solo español (RN-011: el operador aprueba en su idioma) y la traducción downstream ocurre sin acoplamiento directo entre los 2 services. Nuevo `AdminTourTranslationController` con endpoint `POST /admin/tours/{tourId}/retranslate` (solo ADMIN, patrón consistente con `AdminJobsController`) para: (a) backfill de tours legacy creados antes del deploy sin en/pt, (b) testing end-to-end desde Postman, (c) recovery si el listener original falló (Google 500, ADC roto en dev, etc.). Nuevo `MockTranslationClient` para tests (`disable()`, `failNext()`, traducciones sintéticas `[lang] text` para asserts). 17 unit tests: `TranslationServiceTest` (9/9 — happy path, disabled, API fail, blank text, batch multi-lang, batch parcial, empty langs, pt→pt-BR mapper) + `TourTranslationApplierTest` (8/8 — happy path recorre todos los campos, guardrail no sobrescribe, skip cuando disabled, skip cuando tour no existe, batch minimizado que solo pide el idioma faltante, no dispara evento otra vez). **Cero migración BD** (los JSONB ya existen desde el diseño original — no hace falta ni índice ni tracking extra). Nueva dependencia Maven `com.google.cloud:google-cloud-translate:2.43.0`. `application.properties` +4 keys (`agents.translation.enabled`, `agents.translation.project-id`, `agents.translation.source-lang`, `agents.translation.target-langs`). Costo estimado: **$0/mes** con volumen actual (10-80 tours/mes cae dentro de la cuota gratuita 500.000 caracteres); pasada la cuota ~$0.0002/tour. Build verde (`./mvnw clean package -DskipTests`), tests verdes (17/17 nuevos, 100/100 total del paquete unit tests — el `TouryaApiApplicationTests.contextLoads` sigue fallando en CI local por falta de env vars DB, deuda pre-existente no introducida por este sprint). Docs 00, 11, 16, 17 sincronizados. **Cero cambios en `TravelConciergeService`, `AnthropicClient`, ni en `GeminiClient`**. **Cero cambios en el core de `OperatorSupportService`** más allá de cerrar el TODO comentado. **Notas**: docs 05, 12, 14 no se tocan (sin RN nueva, sin cambio de política seguridad, sin cambio alcance mobile) | Franklin | rama `feature/ia-09-google-cloud-translation` |

---

## Changelog

| Versión | Fecha | Cambios |
|---------|-------|---------|
| 1.0 | 2026-06-27 | Generación inicial completa basada en código existente |
| 1.1 | 2026-06-28 | Consolidación con aportes de Luis: visión/mercado (San Andrés), metas 12m, políticas de cancelación, refresh tokens por rol, refactor `Tour.percentageTourya` e `isUnlimitedCapacity`, moderación IA, webhook Wompi, KYB docs obligatorios, eliminación `TourReservation` legacy |
| 1.2 | 2026-06-28 | Nuevo doc `14-gap-web-mobile.md` — análisis funcional web vs mobile y propuesta de matizar la meta de iso-funcionalidad |
| 1.3 | 2026-07-06 | Nuevo doc `15-mvp-mobile-estado.md` — gap analysis del código MAUI actual contra la matriz del doc 14 + roadmap por ciclos |
| 1.4 | 2026-07-06 | Doc `14-gap-web-mobile.md` reescrito v2.0 con el alcance definido por Luis (operadores en la calle → mobile cubre crear tour, schedule y operarios). Doc `15-mvp-mobile-estado.md` actualizado en consecuencia. Doc `16-agentes-ia.md` esqueleto (Luis creó el placeholder, pendiente el contenido detallado) |
| 1.5 | 2026-07-07 | Luis completó `16-agentes-ia.md` con 5 agentes core, arquitectura y roadmap. Aclaración de Luis sobre `isUnlimitedCapacity`: es una LIMPIEZA (eliminar el campo de `tour_schedule`), no una migración. Ajustados docs 04, 05 y 08 en consecuencia. Correcciones ortográficas menores en 14 y 16 |
| 1.6 | 2026-07-07 | Consolidación de las 10 respuestas de Luis por WhatsApp: RN-005 (refresh tokens definidos con OWASP), RN-013 (galería propuesta a validar), RN-015 (percentageTourya aprobado), RN-022 (holdMinutes configurable aprobado), RN-025 (webhook Wompi aprobado), RN-030 (razones nuevas aprobadas). Decisión: WhatsApp vía Twilio (no Meta directo). Validaciones técnicas: `agent_audit_log` necesario, alertas Cloud Monitoring como prerequisito. Presupuesto agentes $100-200/mes aprobado. Actualizados docs 05, 11, 12, 13, 16 |
| 1.7 | 2026-07-07 | Nuevo doc `17-backlog-implementacion.md` — consolida todo el trabajo pendiente en 90+ items priorizados (P0-P3) distribuidos en 6 áreas (seguridad, backend, frontend web, mobile, infra, agentes IA). Plan por 5 fases con cronograma estimado (Fase 1 MVP → 2026-08-18) |
| 1.8 | 2026-07-07 | Ajustadas estimaciones del backlog al modelo de ejecución real (agente IA implementador + Franklin como revisor). Cronograma comprimido: **MVP en producción ~2026-08-03** (3 semanas en vez de 6). Se explicitan los techos no comprimibles: Twilio WABA (2d-2sem), Play Store, testing con turistas reales |
| 1.9 | 2026-07-08 | Fase 0 completada en dev — 5 fixes de código (SEC-03/04/05/08/11), migración de secretos runtime a GCP Secret Manager, y migración de autenticación GitHub Actions → GCP a Workload Identity Federation. Doc 12 actualizado con estado real de vulnerabilidades CRITICAL y HIGH |
| 2.0 | 2026-07-08 | **🎉 Fase 0 100% cerrada en dev**: código (5 PRs), secretos, WIF, alertas Cloud Monitoring (7/8) y backups Cloud SQL habilitados. Docs 05, 07, 11, 13, 17 sincronizados con estado real. Listos para Fase 1 |
| 2.1 | 2026-07-08 | **Fase 1 — 3 bloques cerrados en dev**: BE-16/17 (Wompi webhook + job de reconciliación) y BE-12/13/14/15 (refresh tokens con rotación + detección de reuso). Verificaciones end-to-end incluyendo webhook Wompi real desde Sandbox. Docs 05, 07, 09, 11, 12, 13, 17 sincronizados con estado real |
| 2.2 | 2026-07-09 | **Fase 1 — 4to bloque cerrado**: BE-06/07/08/09 configs (holdMinutes, buffer payout, expiración crédito) migradas a `app_config` con endpoint admin `PUT /config/{key}`. RN-022, RN-036, RN-040 actualizadas de "a implementar" a "implementado". Docs 05, 09, 17 sincronizados |
| 2.3 | 2026-07-09 | **Fase 1 — 5to bloque cerrado**: BE-10 validador RN-013 aplicado en endpoints existentes de galería (formato, tamaño, orientación, ancho mínimo, cuenta). Umbrales en `app_config` (GALLERY_MAX_SIZE_MB, GALLERY_MIN_WIDTH_PX, GALLERY_MAX_IMAGES_PER_TOUR). RN-013 actualizada de "propuesta a validar" a "implementado". Docs 05, 09, 17 sincronizados |
| 2.4 | 2026-07-09 | **Fase 1 — 6to bloque cerrado**: BE-11 RN-045 documentos KYB obligatorios con feature flag `KYB_REQUIRE_MANDATORY_DOCS` (default OFF). RN-045 actualizada de pendiente a implementado con detalle de activación por config y nota sobre providers históricos. Docs 05, 09, 17 sincronizados |
| 2.5 | 2026-07-09 | **Fase 1 — 7mo bloque cerrado**: BE-01/02 reactivar `tour.porcentaje_tourya` como default heredable en creación de slots. RN-015 actualizada de "aprobado — a implementar" a "implementado" con detalle del rename descartado. Doc 08 modelo de datos: nota sobre "agregar campo" cambia a "reactivado 2026-07-09". Docs 05, 08, 17 sincronizados |
| 2.6 | 2026-07-09 | **Fase 1 — 8vo bloque cerrado**: SEC-09 rate limiting en `/auth/**` con feature flag `AUTH_RATE_LIMIT_ENABLED` (default OFF). H-3 marcado como resuelto en doc 12. Limitación de coordinación entre réplicas documentada. Docs 09, 12, 17 sincronizados |
| 2.7 | 2026-07-09 | **Fase 1 — 9no bloque cerrado**: SEC-10 account lockout con backoff exponencial y feature flag `AUTH_LOCKOUT_ENABLED` (default OFF). Complementa SEC-09 con bloqueo por cuenta. H-4 marcado como resuelto en doc 12. Docs 09, 12, 17 sincronizados |
| 2.8 | 2026-07-09 | **Fase 1 — 10mo bloque cerrado**: BE-05 nuevas razones de cancelación `LEGAL_OBLIGATIONS` y `CHANGE_OF_PLANS` (RN-030). Sin migración SQL. Docs 05, 17 sincronizados |
| 2.9 | 2026-07-09 | **Fase 1 — 11vo bloque cerrado (primer FE)**: FE-06 UI de las 2 nuevas razones en `provider-tour-management` (Angular). Bucle end-to-end BE-05 → FE-06 cerrado en el mismo día. Nota adicional: deploy del front ahora va a Cloud Run (no AWS EC2 como decía la doc anterior). Docs 05, 17 sincronizados |
| 2.10 | 2026-07-10 | **Fix crítico infra: migración de tourya-front a WIF**. Se descubrió al mergear FE-06 que el workflow del front seguía usando el key JSON eliminado hace 2 días; todos los deploys fallaban silenciosamente. Mismo patrón WIF que backend + binding IAM para el repo del front. FE-06 finalmente desplegado (revisión `00024-b6m`). Docs 00, 13 sincronizados |
| 2.11 | 2026-07-10 | **Fase 1 — 12vo bloque cerrado (segundo FE)**: FE-04 UI ADMIN de `app_config` en Angular. Standalone component integrado al Dashboard existente (patrón `toggleX`, no rutas nuevas). Edita en caliente 12 configs en 5 grupos, cubriendo todos los flags que introdujo el backend en la Fase 1 (BE-06/07/08/09/10/11 + SEC-09/10). ADMIN puede activar rate limit y lockout sin re-deploy. `CANCELLATION_POLICY` explícitamente excluido del v1. Docs 00, 17 sincronizados |
| 2.12 | 2026-07-10 | **Fase 1 — 13vo bloque cerrado (tercer FE)**: FE-03 validador pre-upload de galería en Angular. Cierra el bucle BE-10 ↔ FE-03: umbrales leídos de `app_config` (mismos 3 keys que edita el panel FE-04), Swal con lista de issues traducida (incluye issues del backend si logran llegar). RN-013 marcada como UI implementada en doc 05. Docs 00, 05, 17 sincronizados |
| 2.13 | 2026-07-10 | **Migración SMTP backend a Workspace Tourya en dev**: rotación de secret + env var `MAIL_USERNAME` en Cloud Run `tourya-dev-api`. Fin de la deuda con Workspace de WASS en dev. Doc 11 actualizado con nueva config; pendientes en prod (SPF/DKIM/DMARC + creación de `noreply@tourya.co`) documentados. |
| 2.14 | 2026-07-10 | **Fase 1 — 14vo bloque cerrado (cuarto FE)**: FE-01 auto-refresh transparente en `AuthInterceptor` con mutex + `logout()` server-side. Cierra el bucle FE↔BE con BE-12/13/14/15. Se optó por localStorage (opción B) sobre cookie HttpOnly — decisión documentada, FE-01b queda como item futuro. Revisión desplegada `tourya-dev-front-00027-9pl`. Docs 00, 05, 17 sincronizados |
| 2.92 | 2026-08-15 | **IA-09 Traducción automática es→en/pt-BR con Google Cloud Translation v3 (rama `feature/ia-09-google-cloud-translation`)**. Tercer bloque IA productivo (tras IA-02 Travel Concierge y IA-07 Operator Support) y cierra el TODO deferred que IA-07 había dejado. Nuevo paquete `com.tourya.api.services.translation` con `ITranslationService` (interface con contrato "nunca throws" — devuelve `null` si el service está apagado o falla) + `GoogleCloudTranslationService` (v3 basic, auth ADC, mismo patrón lazy-init + double-checked-locking que `GeminiClient`). Hook async en `TourService.saveCreateOrUpdateFullData` vía `TourTranslationEvent` + `TourTranslationEventListener` (`@TransactionalEventListener(AFTER_COMMIT)`) + `TourTranslationApplier.translateTourAsync` (`@Async @Transactional`) — mismo patrón que `CreditRefundEventListener` v2.83. El applier recorre todos los campos JSONB del tour (`name`, `description`, `TourAddress.location`, `TourMainAttraction`, `TourIncludesExcludes`, `TourFaq.question`+`answer`, `TourItinerary.title`+`description`, `TourCancellationPolicy.observations`, `TourGallery.description`) y respeta el guardrail RN-011: solo rellena en/pt vacíos, nunca sobrescribe lo que el provider escribió manualmente. Convención `pt` en el JSONB (mismo que el resto del proyecto, verificado con grep en migración 056), internamente mapeado a `pt-BR` para el call a Google (portugués de Brasil por decisión de Luis en `traduccion-automatica-tours.md`). Nuevo `AdminTourTranslationController` con `POST /admin/tours/{tourId}/retranslate` (solo ADMIN) para backfill de tours legacy + testing end-to-end + recovery de listeners fallidos. Nueva dep Maven `com.google.cloud:google-cloud-translate:2.43.0`. 4 configs nuevas en `application.properties` (`agents.translation.enabled`, `project-id`, `source-lang`, `target-langs`). API `translate.googleapis.com` habilitada en `tourya-project-dev` + rol `roles/cloudtranslate.user` a la SA Cloud Run el mismo día. 17 unit tests con nuevo `MockTranslationClient` (9 en `TranslationServiceTest` + 8 en `TourTranslationApplierTest`), cero llamadas a Google reales. **Cero migración BD** (los JSONB ya existen). Wire-up con IA-07: TODO cerrado, sin acoplamiento directo entre los services — el hook vive en `TourService` y no en `OperatorSupportService`. Costo estimado: **$0/mes** con volumen actual, ~$0.0002/tour pasada la cuota gratuita. Build verde, tests verdes 17/17 nuevos + 100/100 unit tests totales. Docs 00, 11, 16, 17 sincronizados. Docs 05, 12, 14 NO se tocan (sin RN nueva, sin cambio seguridad, sin cambio alcance). Cero cambios en `TravelConciergeService`, `AnthropicClient`, `GeminiClient` |
| 2.91 | 2026-08-15 | **Sprint 9 FE + MO Operator Support — widgets Angular + MAUI del Agente 4 (tourya-front PR #119 mergeado + tourya-mobile local commit `b6de801` + merge `cc45149`)**. Cierra el ciclo Sprint 9 end-to-end: backend IA-02 fue chat (Sprint 8), backend IA-07 son 4 endpoints action-specific (v2.90), y ahora los 4 touchpoints aditivos aterrizan en web y mobile del provider. **Cero regresión** — si el provider no toca los botones `✨`, todos los flujos existentes funcionan igual. Ambos clientes consumen los 4 endpoints de `/agents/operator-support/*` con auth JWT rol PROVIDER/PROVIDER_OPERATOR. **FE Operator Support Angular** — nuevo `OperatorSupportService` + models en `src/app/shared/{services,models}/`. **Touchpoint 1** (sugerir contenido): botón `✨ Sugerir con IA` en el header de la sección Descripción del `add-tour` (component monolítico, no wizard) → modal con 3 secciones (nombres + descripción + tags multi-select), todo requiere click explícito. **Touchpoint 2** (price alert): badge único al top de la sección Prices de `tour-schedule` (multi-slot × multi-age — endpoint idempotente por tourId), refrescado on-blur, colores por severity (verde OK / amarillo WARN / rojo CRITICAL). **Touchpoint 3** (validate-gallery): pre-upload en `tour-gallery` con metadata local (`img.onload` para `naturalWidth`/`naturalHeight` + `file.size`/`file.type`) → renderiza issues estructurados inline + bloquea si CRITICAL con Swal + fallback silencioso si endpoint no responde. **Touchpoint 4** (draft-review-reply): botón `✨ Borrador con IA` en cada card de reseña sin responder de `provider-reviews` → pre-llena el TextEditor existente con el draft + label chico "Tono detectado". 20 keys i18n `operatorSupport.*` en `public/i18n/{es,en,pt}.json`. Build production verde 0 warnings nuevos. Adaptaciones vs brief: convención real del repo (`src/app/shared/{services,models,common}/`), `add-tour` es monolítico no multi-step, `currentTourId` de private → public en TS strict, `cartId` derivado del JWT server-side. **MO Operator Support MAUI** — nuevo `IOperatorSupportService` + `OperatorSupportService` reusando `ApiService.PostAsync<TRequest, TResponse>` (mismo patrón que `ConciergeService`). DI singleton en `MauiProgram.cs`. **Descubrimiento estructural clave**: en mobile los precios del tour viven en `ScheduleTemplateForm` (no en `TourFormPage` como el web) — el agente descubrió esto y adaptó el brief: puso el price-alert donde tiene sentido, en el form de plantilla donde el provider realmente edita precios. **Touchpoint 1**: botón "✨ Sugerir con IA" en el step Descripción del `TourFormPage` (wizard multi-step) → nueva `OperatorSuggestModalPage` modal en `Views/Agents/` con 3 secciones apilables. **Touchpoint 2**: banner con `DataTriggers` OK/WARN/CRITICAL en la parte superior del `ScheduleTemplateForm` + `EventToCommandBehavior` con `EventName="Unfocused"` en cada Entry de Price. **Touchpoint 3**: pre-upload usando `SKBitmap.Decode` de SkiaSharp (ya está por MO-31) para leer metadata local + `ValidateGalleryAsync` server-side + fallback silencioso. **Touchpoint 4**: botón "✨ Borrador con IA" en `ProviderReviewsPage` (Sprint 5a) → pre-llena el TextEditor existente + label "Tono detectado". 21 keys i18n `operatorSupport.*` en `I18nService`. **Patrón comandos MAUI**: `AsyncRelayCommand` como propiedad explícita en el constructor (NO `[RelayCommand]`) — blueprint del Sprint 8 MO-Concierge conservado. Baseline **140 warnings intacto**, 0 warnings nuevos, 0 errores. **Nota flow MO**: el agente stopped por corte de proceso alcanzó a hacer todo el trabajo (14 archivos, 803 líneas) sobre `develop` compartido en el checkout principal (sin worktree ni rama). Recovery manual: build verde 0/0 → rama `feature/mobile-sprint9-operator-support` → commit del working tree → merge local no-ff a `develop` → cleanup rama. **Deudas menores** (no bloqueantes): alineación tipos `subcategoryId: number` en FE + MO Tour DTO, `reviewId: number` en MO Review DTO, `InsufficientPrivilegesException` 401 vs 403 en backend (deuda pre-existente). **Provider ahora con asistencia IA en el flujo entero**: nombre/descripción/tags del tour, alerta de pricing vs mercado, validación de galería pre-upload, borrador de respuesta a reseñas. Docs 00, 15, 17 sincronizados. **Notas**: doc 05 no se toca (sin RN nueva), doc 12 tampoco (guardrails ya documentados en doc 16), doc 14 tampoco (IA-07 es refinamiento de features existentes), doc 16 ya se actualizó en v2.90 |
| 2.90 | 2026-08-14 | **IA-07 Operator Support backend — 4 endpoints action-specific bajo `/agents/operator-support/*`**. Segundo agente IA productivo del bloque doc 16 (después de IA-02 Travel Concierge). Servicio `OperatorSupportService` con 4 métodos: `suggestTourContent` (nombres + descripción SEO en español + tags del catálogo), `priceAlert` (severity + rango de comparables — heurística fallback +/-15% / ±35%, RN-014 solo alerta), `draftReviewReply` (borrador + tone + detectedLocale, operador aprueba antes de `PATCH /public/save/review/{reviewId}`), `validateGallery` (cero LLM, reusa reglas de `GalleryValidator` sobre metadata pre-upload). Modelo Gemini 2.5 Pro (mismo motivo que IA-02: reusa GCP infra, ~40% más barato que Sonnet). Costo esperado $1-2/mes (baja vs $2-4 con Sonnet). Guardrails idénticos a IA-02 (deny-list secretos + scrub `providerPrice`/`slotPercentageTourya` + budget guard + audit siempre) + autorización owner-based (`requireTourOwnership` valida antes de cualquier call). Nuevo `OperatorSupportRepository` con 2 native queries usando `price` público (nunca `provider_price` interno entre operadores). 3 prompts versionados `prompts/operator-support/*.v1.txt`. 4 DTOs (`TourContentSuggestion`, `PriceAlert` con enum Severity, `DraftReviewReplyResponse` con enum Tone, `ValidateGalleryResponse`). Nuevo `OperatorSupportController` bajo `/agents/operator-support/*` (separado de `AgentController` para claridad de responsabilidad). Feature flag `agents.operator.enabled=${AGENTS_OPERATOR_ENABLED:true}`. **Traducción es→en/pt DEFERRED a IA-09**: TODO explícito en el service, es 1 wire-up cuando exista. 11 unit tests con `MockLlmClient` (happy paths + malformed JSON + auth 401 + guardrails deny-list + guardrails scrub + heurística sin LLM). Cero cambios en tabla `agent_audit_log` (reusa migración 092 de IA-02). Cero cambios en `TravelConciergeService` / `AnthropicClient`. Build verde, tests verdes 33/33 en el paquete IA. Docs 00, 09, 16, 17 sincronizados. Docs 05 y 12 NO se tocan (sin RN nueva, guardrails ya documentados). **Autorización 401 no 403**: consistente con `InsufficientPrivilegesException` global del proyecto |
| 2.89 | 2026-08-14 | **Sprint 8 FE + MO Concierge — widgets Angular + MAUI del Travel Concierge (tourya-front PR #118 mergeado + tourya-mobile local commit `7c76f39` + merge `190f1ba`)**. Cierra el ciclo Sprint 8 end-to-end: backend IA-02 (v2.88) + web (FE-Concierge) + mobile (MO-Concierge) en el mismo día. Ambos clientes consumen `POST /agents/travel-concierge/chat` del backend IA-02 con: `sessionId` UUID persistido (web: `sessionStorage` para multi-tab separadas; mobile: `SecureStorage` para sobrevivir app restart), locale desde el i18n service actual, envelope estándar `ApiResponse<T>`. **FE-Concierge Angular** — nuevo `TravelConciergeService` + `ConciergeChatWidget` (FAB flotante bottom-right expandible a panel 380×500 desktop / full-screen mobile <640px) con historial burbujas usuario/asistente + chips clickeables ("🔍 3 tours encontrados", "✅ Agregado al carrito") + banner amarillo `escalatedToHuman`. Integrado en 3 páginas: `list-tours` (Explore, sin context), `tours-detail` (con `tourId`, `currentTourId` de private → public para `strictTemplates=true`), `cart-summary` (sin `cartId` — backend lo deriva del JWT). 10 keys i18n `concierge.*` en `public/i18n/{es,en,pt}.json`. Build production verde, 0 warnings nuevos. Adaptaciones vs brief: convención real del repo (`src/app/shared/{services,models,common}/` con `standalone: false` en `SharedModule`, no `src/app/{services,models,components}/` standalone) e i18n en `public/i18n/` no `src/assets/i18n/`. **MO-Concierge MAUI** — nuevo `IConciergeService` + `ConciergeService` (session en `SecureStorage`, locale de `I18nService`, POST vía `ApiService.PostAsync<T,R>`), `ConciergeChatPage` modal con `CollectionView` + Entry + `ActivityIndicator` + chips + banner. FAB "Concierge Tourya" en `TourDetailPage.xaml`/`CartPage.xaml`/`ExplorePage.xaml`, ruta `concierge` en `AppShell.xaml` fuera de tabs, DI singleton service + transient VM/Page. **Rotación de session en logout**: `AuthService.LogoutAsync` invoca `_storage.RemoveConciergeSessionIdAsync()` ANTES de `ClearAllAsync` (defense-in-depth doble llamada) — próximo user arranca con hilo fresco. 10 keys i18n `concierge.*` en `I18nService`. **Divergencia patrón**: 2 comandos nuevos como propiedades explícitas `new AsyncRelayCommand(...)` en el constructor en vez de `[RelayCommand]` — necesario para respetar baseline 140 warnings (RelayCommand + XAML source-gen no se coordinan → MAUIG2045). Blueprint para cleanup futuro de los ~100 MAUIG2045 pre-existentes. Build verde 0 errores, 140 warnings baseline conservado. **Turista mobile cerró el último ⚠️ parcial del doc 14** ("Buscar tours con Travel Concierge (IA)") → ahora ✅. **100% del alcance mobile del doc 14 cerrado.** Docs 00, 15, 17 sincronizados. **Notas**: doc 05 no se toca (no hay RN nueva) y doc 12 tampoco (los guardrails ya están documentados en 16 y el patrón secretos-jamas-al-LLM no cambia políticas de seguridad) |
| 2.88 | 2026-08-14 | **IA-02 Travel Concierge backend + switch a Vertex AI Gemini — tourya-api PR #263 mergeado**. **Migración 092 aplicada a Cloud SQL dev el 2026-08-14 antes del merge** con el patrón docker+psql documentado (Franklin reforzó regla operativa: "aplicar migración SIEMPRE antes del merge, nunca al revés"). Primer agente IA real del bloque (doc 16 §Agente 1) cerrado. Endpoint `POST /agents/travel-concierge/chat` (JWT USER, envelope directo DTO) con `TravelConciergeService` que corre loop function-calling (max 5 iter) contra `TourService`/`ShoppingCartService`/`SearchTourScheduleFullService` — 4 funciones: `search_tours`, `get_tour_detail`, `add_to_cart`, `get_cart`. **Cambio de LLM provider Anthropic → Vertex AI Gemini** (razones en doc 16 nueva seccion "Por que Gemini"): reusa GCP infra + SA `roles/aiplatform.user` + auth ADC (cero API key nueva), ~40% mas barato, `ILlmClient` ya provider-agnostic desde IA-01 → cambio aditivo, `AnthropicClient` sobrevive como impl alternativa via `AGENTS_PROVIDER=anthropic`. Nuevo `GeminiClient implements ILlmClient` (lazy init de `VertexAI`, modo no-op degradado, nunca throws) + `LlmClientConfig` con `@Primary` selector por `agents.provider`, entradas `gemini-2.5-pro` ($1.25/$10) y `gemini-2.5-flash` ($0.075/$0.30) en `ModelPricing`. Guardrails en codigo (no solo prompt): deny-list de secretos en `userMessage` (`WOMPI_INTEGRITY_SECRET`, `JWT_SECRET`, `ANTHROPIC_API_KEY`, `WOMPI_EVENTS_SECRET`, `FIREBASE_ADMIN_SDK_JSON`) → escala humano + audit `rejected` sin llamar LLM; scrub recursivo de `providerPrice`/`slotPercentageTourya`/`slotPorcentajeTourya`/`porcentajeTourya` del contexto pre-envio (defense-in-depth aunque los DTOs actuales ya no los exponen); contador `ConcurrentHashMap<sessionId, int>` de pagos fallidos con umbral 3 → `fraud_suspected=true` en metadata, jamas comunicado al turista. Migracion 092 (NO aplicada a Cloud SQL dev, Franklin la corre) agrega `metadata JSONB` + `GIN` index a `agent_audit_log` para persistir `session_id`, `fraud_suspected`, `actions_executed[]`, `escalated_to_human`. Nuevo `MockLlmClient` para tests (queue de responses hardcoded + capturedPrompts para asserts) + 9 unit tests `TravelConciergeServiceTest` cubriendo los 5 escenarios del brief (consulta simple, `search_tours`, `add_to_cart`, guardrail secretos, scrub) + budget guard exhaustion + fraud counter tras 3 fallos. Nueva dependencia Maven `com.google.cloud:google-cloud-vertexai:1.14.0`. `application.properties` +3 keys (`agents.provider`, `agents.gemini.enabled`, `agents.gemini.project-id`, `agents.gemini.location`). Prompt versionado `resources/prompts/concierge.v1.txt` cargado via `PromptTemplate.load`. Costo estimado recalculado con Gemini: **$8-12/mes** para Travel Concierge (contra $20-25 con Anthropic). Build verde (`./mvnw clean package -DskipTests`), tests verdes (`./mvnw test -Dtest=TravelConciergeServiceTest` 9/9). Docs 00, 09, 16, 17 sincronizados. **Nota**: doc 05 no se toca (no hay RN nueva) y doc 12 tampoco (guardrails de secretos son en codigo, no nueva politica de seguridad). Infra GCP habilitada por Franklin el mismo dia (Vertex AI API + SA rol aiplatform.user en `us-central1`) |
| 2.87 | 2026-08-14 | **Sprint 7 mobile — offline extendido operario (MO-56) — tourya-mobile local**. Cierre del ítem "Modo offline extendido" del Ciclo 5 del roadmap doc 15. Extiende `IReservationCacheService` (que en MO-50 solo cachea el Dashboard) para cachear también la lista paginada del `ProviderReservationsPage` — el operario que trabaja en el bote o el muelle sin señal ahora puede seguir viendo TODAS las reservas que ya cargó, no solo las del día del Dashboard. Nuevo path `FileSystem.CacheDirectory/provider_reservations_page.json` (separado del `reservations_today.json` de MO-50 para no colisionar). Save-on-success en cada `LoadMore` exitoso; fallback-on-error activa `IsOfflineMode=true` + banner amarillo arriba del CollectionView reusando el estilo del Dashboard MO-50 con texto "📴 Modo offline · última actualización hace X min" (o "hace unos segundos" si <1 min); `_hasMorePages=false` en el fallback evita que `LoadMoreCommand` intente paginar contra un backend caído (pull-to-refresh reinicia el flow limpiamente al volver la señal). 2 keys i18n `reservation.offline.banner` (con placeholder `{0}` = minutos) y `reservation.offline.banner.recent` en es/en/pt. Merge local commit `34d32ae` → merge `29dd8d6`. **Cero cambios backend**. **Item 2 del Sprint 7 (sub-grouping por franja horaria mañana/mediodía/tarde) NO se implementó** — auditoría confirma que `ProviderReservationsViewModel.RebuildFiltersAndGroups` ya ordena `OrderBy(ScheduleDate).ThenBy(SlotTimeStart).GroupBy(ScheduleDate)` (agrupación por día + orden ascendente por hora dentro del día) desde MO-51, pero sin sub-grupos por franja horaria; decisión pendiente Franklin si vale una iteración corta MO-56b para agregar mañana/mediodía/tarde. Deudas técnicas descubiertas: (a) baseline warnings mobile es 140 no 139 como decía la memoria del prompt (verificado con stash + rebuild limpio); (b) 2 warnings pre-existentes MAUIG2045 en `ProviderReservationsPage.xaml` por `LoadReservationsCommand`/`LoadMoreCommand` (source generator no las ve — no introducidas por este sprint). Docs 00, 15, 17 sincronizados. **Nota**: doc 05 no se toca (no hay RN nueva) y doc 09 tampoco (sin endpoints backend nuevos — todo es client-side caching sobre `GET /provider/reservations` que ya existía) |
| 2.86 | 2026-08-14 | **Sprint 6 mobile — MO-53 (reagendar turista 3 casuísticas) + MO-54 (mustChangePassword operario) + WompiHelper cleanup — tourya-mobile local**. Cierre en bloque de las 2 deudas Sprint 5b + hardening del checkout. **Sprint 6a MO-54** (commit `8f50f1b` + merge `8693c97`) — cierra security issue moderado del operario. Backend ya expone `AuthenticationResponse.mustChangePassword` + endpoint self-service `PATCH /users` (`UserController.java:30-38`) — cero cambios backend, primer consumo desde mobile. Mobile agrega: campo `mustChangePassword` en `AuthResponse`, guard en `LoginViewModel` que navega a `//change-password` cuando el flag viene `true` bloqueando el home, nueva `ChangePasswordPage` + `ChangePasswordViewModel` + `ChangePasswordRequest` DTO, nuevo helper `PatchAsync<TRequest>` en `ApiService`, `AuthService.ChangeMyPasswordAsync`, ruta `change-password` en `AppShell.xaml`, DI en `MauiProgram.cs`, 11 keys i18n `auth.changePassword.*` (es/en/pt). El operario recién creado por PROVIDER (MO-24) ya no puede navegar sin cambiar la clave temporal. **Sprint 6b MO-53** (commit `58d7a4f` + merge `1af997a`) — cierra reagendar 3 casuísticas turista (RN-033). Backend ya expone `PUT /reservations/{id}/reschedule` (ReservationController:387) — cero cambios backend, primer consumo desde mobile. **Sorpresa del contrato**: para casuística HIGHER el backend **NO** abre Wompi directo por el delta; cancela la reserva anterior, abre crédito por el valor pagado y agrega el nuevo tour al carrito (response `priceComparison=HIGHER` + `status=CANCELLED_AND_ADDED_TO_CART`). El turista completa el pago vía checkout normal del carrito — **mismo patrón que web**, intencional del backend. Mobile agrega: `IReservationService.RescheduleReservationAsync`, DTOs `RescheduleReservationRequest`/`Response` con `priceComparison` (EQUAL/LOWER/HIGHER) en `Models/Reservation/`, botón "Reagendar" en `ReservationDetailPage` visible sólo si `canReschedule==true`, nueva `RescheduleReservationPage` + `RescheduleReservationViewModel` con lista de días disponibles + dispatch por casuística (EQUAL/LOWER → alert de confirmación + volver a Mis Viajes; HIGHER → alert explicando el patrón carrito + navigate `//tourist/cart`), 12 keys i18n `reservation.reschedule.*` + `common.ok`. **WompiHelper cleanup** (commit `2cecbb2` + merge) — cierre técnico del hardening abierto por Sprint 4a: reemplaza `EscapeJs` manual (que sólo escapaba `'` y newlines) por `System.Text.Json.JsonSerializer.Serialize` para cubrir XSS/escape completo (`<`, `\`, `</script>`, unicode, etc.) del user-input del turista embebido en el HTML/JS de `WompiHelper.BuildCheckoutHtml`; `redirectUrl` pasa de hardcode a prod → `Constants.WebBaseUrl` (dev-aware, coherente con MO-01b). `CheckoutViewModel.cs:176`. Docs 00, 15, 17 sincronizados. **Nota**: doc 05 no se toca (no hay RN nueva) y doc 09 tampoco (los endpoints `PATCH /users` y `PUT /reservations/{id}/reschedule` ya existían en backend — este sprint es primer consumo desde mobile) |
| 2.85 | 2026-08-14 | **Sprint 5a responder reseñas provider mobile + auditoría Sprint 5b + 2 deudas nuevas MO-53 / MO-54 — tourya-mobile local**. **Sprint 5a**: nueva `ProviderReviewsViewModel` + Page con flujo completo de responder reseñas (TextEditor + Enviar/Cancelar + nuevo `StringNotEmptyToBoolConverter` + 7 keys i18n es/en/pt) reusando `ReviewService.ReplyToReviewAsync` (existía desde MO-20 con el fix del multipart, sin cambios backend ni service). Cierra el ❌ del doc 14 "Responder reseñas provider" → ahora ✅ en el doc 15 Gap Analysis provider. **Sprint 5b auditoría** de los 3 ítems ⚠️ del doc 14: (1) **Reagendar reserva turista 3 casuísticas** → ❌ NO implementado. `ReservationDetails` recibe `MaxReschedulingDate` + `CanReschedule` pero mobile los ignora, `IReservationService` sin método Reschedule, `ReservationDetailPage` sin botón. Backend RN-033 ya soporta los 3 casos (BE-20). Registrado como **MO-53** (~4-6h, P1). Doc 15 fila actualizada ⚠️ → ❌. (2) **Confirmar reserva manualmente fallback QR** → ✅ YA implementado en `QrScannerPage.xaml:29-34` (botón "Ingresar manualmente" + toggle) + `QrScannerViewModel.cs:164-183` (`ProcessManualEntryAsync` reusa el pipeline QR con guard TC-007). El doc 15 decía "En ProviderReservationsPage" — ubicación incorrecta; vive en `QrScannerPage` (UX correcto: el operador está scaneando ahí). Docs 15 líneas 97 y 115 corregidas ⚠️ → ✅. (3) **Login `mustChangePassword` operario** → ❌ NO implementado. `AuthResponse` DTO sin el campo, `LoginViewModel.LoginAsync` navega al home sin chequear, no existe `ChangePasswordPage` ni endpoint self-service. Solo `ProviderOperatorService.ResetPassword` (MO-24) para que PROVIDER resetee — no self-service del operario. **Security issue moderado**: operario recién creado usa clave temporal sin forzar cambio. Registrado como **MO-54** (~3-4h, P1). Doc 15 fila actualizada ⚠️ → ❌. Docs 00, 15, 17 sincronizados |
| 2.84 | 2026-08-14 | **Sprint 3 A/B mobile (autosave + upload background + calendar + copy prices) + Sprint 4a autoVerify + WompiHelper cleanup — tourya-mobile local + tourya-front PR #117**. **Sprint 3 A** (cierra MO-30 + MO-32): nuevo `TourDraftService` persiste el estado del wizard como snapshot JSON en `AppDataDirectory/tour_draft.json` (recovery on crash) + nuevo `TourGalleryUploadService` con `SemaphoreSlim(3)` (3 uploads paralelos máx) + progress per-item (0-100%) + retry en fallo transitorio + `TourFormPage` extendido con Step 7 Galería que muestra progress bar por imagen. El wizard ya no bloquea al provider mientras suben las imágenes. **Sprint 3 B** (cierra MO-33 + completa alcance original MO-34): `ScheduleCalendarPage` con `SfCalendar` (Syncfusion) + toggle Día/Semana/Mes vía botones + special dates predicate; botón "💰 Copiar precios de otro tour" en `ScheduleTemplateFormPage` reusando endpoint TC-019 `GET /tour-schedules/templates?tourId={id}` via nuevo `ScheduleService.GetTemplatesForTourAsync` — filtra plantillas por tour destino (antes MO-34 en 2026-07-15 quedó como "Copiar de otra plantilla" mostrando TODAS porque el response no incluía `tourId`). **Sprint 4a** (cierra MO-43b): `MainActivity.cs` con `AutoVerify=true` en el `IntentFilter` del deep-link `https://dev.tourya.co/clients/tours-detail/*`. Android abre la app **sin chooser**. Depende de `tourya-front` PR #117 mergeado que publica `assetlinks.json` en `public/.well-known/` con el fingerprint SHA-256 del keystore. **WompiHelper cleanup**: reemplazado `EscapeJs` manual (solo escapaba `'` y newlines) por `System.Text.Json.JsonSerializer.Serialize` que cubre XSS/escape completo — cierra vulnerabilidad **moderada** donde user-input del turista podía escaparse del contexto JS del `WompiHelper.BuildCheckoutHtml`; `redirectUrl` ahora usa `Constants.WebBaseUrl` (antes hardcode a URL de prod) — `CheckoutViewModel.cs:176`. Docs 00, 15, 17 sincronizados |
| 2.83 | 2026-08-13 | **TC-022 emails al turista en devolución de crédito — tourya-api PR #257**. Cierra la deuda de comunicación asincrónica del flujo de devolución en efectivo (RN-062). Nuevo package `services.credit.events` con `CreditRefundEvent` (sealed interface + 2 records snapshot: `RefundRequested` y `RefundCompleted`) y `CreditRefundEventListener` (`@Async @TransactionalEventListener(AFTER_COMMIT)`) — mismo patrón que MO-40b: el email jamás se manda si la tx principal hace rollback, y un fallo de SMTP nunca aborta la actualización del estado del crédito (fire-and-forget con `@Async`). Publisher en `CreditService.requestRefund` (email 1: azul "Solicitud recibida") y `uploadRefundProof` (email 2: verde con CTA "Ver comprobante" enlazado a `refund_proof_url`). 2 templates Thymeleaf nuevos en `resources/templates/`: `credit_refund_requested.html` y `credit_refund_completed.html`. Nuevo helper `EmailService.sendCreditRefundRequestedEmail` / `sendCreditRefundCompletedEmail` con render Thymeleaf + `mailSender.send`. **Solo español** (coherente con los templates existentes BE-18/19 de expiración de créditos) — multi-idioma queda como deuda **TC-022c**. **Deuda detectada TC-022b**: el email no menciona el motivo original del crédito porque la entidad `Credit` no tiene columna `reason` — solo `type` (CANCELATION/RESCHEDULE/BONUS). Registrada para PR aparte. Docs 00, 05, 17 sincronizados |
| 2.82 | 2026-08-13 | **3 deudas backend expose fields cerradas + sync doc 09 — tourya-api PRs #255 y #258**. **PR #255**: expone 4 campos al frontend/mobile que estaban en BD pero no en los DTOs — desbloquea los MO-DT16/17/18 detectados en la auditoría del Sprint 2 mobile. (a) `TourFullDataResponse.blockedByMaritimeReport` (boolean opcional) para que el detail del tour deshabilite el CTA "Reservar" sin depender del 400 del carrito, semántica idéntica al helper `MaritimActivityReportRepository.findActiveRedReportsForSubcategoryAndLocation` usado por el guard duro de `ShoppingCartService`; (b) `SearchTourScheduleFullResponse.AddressResponse.addressType` para que search/list-tours identifique tours Hotel Pickup y renderice el chip/mensaje sin round-trip al detail; (c) `ShoppingCartItemResponse.addressType` para el mismo propósito en el carrito; (d) `ReservationResponse.travelerBreakdown` (List<TravelerBreakdownDto>) para el desglose por `ageType` en detalle de reserva del turista (equivale al que ya expuso `sp_get_provider_reservations` en TC-016 parte B para el provider). Nuevo enricher batch `SearchTourAddressTypeEnricher` — un solo query IN-clause post-search para todas las direcciones, sin N+1. Cero migración BD — solo DTOs + mappers + enricher. **PR #258**: sync `docs/09-api-design.md` con los 4 campos nuevos + tabla de endpoints afectados por cada campo. Docs 00, 09, 17 sincronizados |
| 2.81 | 2026-08-13 | **TC-022 devolución de créditos cerrada en los 3 frentes** (issue #253) — Sprint día. **Backend** (PR api #254 + migración 091): 2 estados nuevos en `CreditStatusEnum` (`REFUND_REQUESTED`, `REFUNDED`), 3 columnas nuevas en `credit` (`refund_requested_at`, `refunded_at`, `refund_proof_url`), 3 endpoints (`POST /credits/{id}/request-refund` turista, `POST /admin/credits/{id}/upload-refund-proof` ADMIN/BACKOFFICE multipart JPG/PNG/PDF ≤5MB via `IStorageService`, `GET /admin/credits` paginado global con `touristName/touristEmail` embebidos). Nuevo `AdminCreditController` separado del turista. Comprobante al path `credit-refund-proofs/{creditId}/...` (S3 o GCS según env). Migración 091 aplicada a Cloud SQL dev (`tourya-dev-db`). **Bonus fix**: la mig 091 normaliza el CHECK constraint `credit_status_check` con el catálogo completo (`CREATED, RESERVED, CONSUMED, CANCELED, DELETED, EXPIRED, REFUND_REQUESTED, REFUNDED`) — cierra la deuda latente donde la mig 074 (BE-19) empezó a persistir `'EXPIRED'` sin actualizar el CHECK. **Frontend web** (tourya-front PR #115): turista con botón "Solicitar devolución" + SweetAlert2 confirm en `/clients/my-profile?section=credits`; nueva ruta `/admin/credits` con `AdminCreditsComponent` (tabla paginada + modal upload); item "Créditos" al sidebar admin; i18n ES/EN/PT completo. **Mobile MAUI** (commit local `2d770f8` mergeado a develop): turista con botón + confirm en `CreditsPage`; chip visual REFUND_REQUESTED (amarillo) y REFUNDED (azul) + link "Ver comprobante"; flujo admin queda web-only (coherente con doc 14). **Nueva RN-062** en doc 05. Reembolsos manuales previos quedan fuera del sistema — sin backfill. No reversible por diseño. Docs 00, 05, 08, 09, 12, 14, 17 sincronizados |
| 2.80 | 2026-08-13 | **Sprint 2 mobile cerrado — 9 TCs del ciclo QA agosto portados al MAUI Android**. Todos los TCs del ciclo QA que aplicaban a mobile pasaron a ✅ en la matriz "Gap con ciclo QA agosto" del doc 15. Portados: **TC-009** (documentType tourist_profile — Picker CC/CE/PA/NIT en `ProfilePage` + `CheckoutViewModel` pre-fill desde profile + `AuthResponse.documentType` + endpoint `GET/PUT /users/me/profile`); **TC-014** (auto-purga cart items scheduleDate vencido — detección client-side + alert al turista); **TC-017 display** (Hotel Pickup en 4 vistas mobile: TourDetail, Explore, Cart, ReservationDetail + i18n `priceType` + nueva API `I18nService.T(key)` con tablas es/en/pt); **TC-018** (DIMAR RED bloquea add-to-cart mobile + guard 400 backend + banner rojo); **TC-020** (docType real a Wompi mobile — Picker en `CheckoutPage.xaml` reemplaza hardcode `"CC"` + botón "Ver mis reservas" en confirmación + refresh profile post-tx); **TC-016** (`travelerBreakdown` por ageType — nuevo `TravelerBreakdownConverter` con pluralización via `I18nService`); **TC-021** (placeholder "🔒 Datos disponibles el día del tour" cuando backend scrubbea datos cliente PROVIDER >1 día); **TC-007** (guard temporal QR scanner — solo día del tour Bogota, mapeo de error backend a mensaje user-friendly); **TC-019** (multi-ageType en `ScheduleTemplateFormViewModel` mobile — antes hardcoded `AgeType = "ADULT"` línea 233). **Deudas y decisiones aprobadas**: (P6) **TC-017 creación** con `addressType=HOTEL_PICKUP` en `TourFormPage` mobile: **SÍ** (Luis 2026-08-13) — deuda para futuro ciclo. (P1) **TC-012/013 guest checkout mobile: NO** (Luis 2026-08-13) — mobile obliga cuenta, **Ciclo 7 del roadmap cancelado**. (P2) **TC-019 batch schedule provider mobile** sigue pendiente respuesta Luis. (P3/P4/P5) respondidas Franklin: **MO-01b** URL API a HTTPS `https://dev.tourya.co/api/v1/` **cerrada**; **MO-02** Syncfusion License de Touya Marketplace S.A.S. **registrada**; **iOS confirmado fuera de scope**; repo Git remoto sigue diferido. Build mobile verde (`dotnet build -c Debug -f net10.0-android`, 0 errores, .NET 10.0.300). Mobile alineado 1:1 con web al 13-ago. Docs 15, 17 sincronizados |
| 2.79 | 2026-08-13 | **Auditoria mobile post ciclo QA agosto — doc 15 actualizado**. Cross-reference del estado real de `tourya-mobile` (build verde, 72 archivos untracked, URL sigue en HTTP LB GCP) contra los TCs 007-021 mergeados en `develop` durante ago. Resultado: 0 TCs portados completos, 4 parciales (TC-007/019/020/021), 5 no portados (TC-009/014/016/017/018 + TC-012/013 pendiente decision guest), 3 N/A (TC-008/011/015). Se agregan al doc 15: sección "Estado al 2026-08-13", matriz "Gap con ciclo QA agosto" con archivo mobile a tocar por cada TC, y roadmap actualizado por ciclos 0-7 con MO-DT7/9/14/16/17/18/19b/19c/20/21 como nuevas deudas tecnicas trackeadas. 6 preguntas abiertas para Luis/Franklin (P1 guest checkout, P2 batch schedule, P3 repo Git, P4 Syncfusion license, P5 iOS, P6 addressType en TourFormPage). Sin cambios de codigo mobile — solo doc. Sin migraciones |
| 2.78 | 2026-08-13 | **TC-017 cierre selector orden — quitar "Recomendado" + default vacío — tourya-front PR #114**. Ajuste final del selector "Ordenar por" en `list-tours` tras feedback de Luis (13-ago 12:07): (1) eliminada la opción "Recomendado" del dropdown; (2) default = vacío (ninguna opción seleccionada) — cuando no se elige nada la lista respeta el orden que envía el backend; (3) solo quedan 2 radios: "Precio menor→mayor" / "Precio mayor→menor". Tipo `sortOption` pasa de `'recommended' \| 'price_asc' \| 'price_desc'` a `'' \| 'price_asc' \| 'price_desc'`. 2 archivos: `list-tours.component.{ts,html}` (+10/-15). Sin migraciones ni cambios backend. Cierra así el ciclo TC-017 (8 iteraciones acumuladas) — esperando validación final de Luis del deploy. Docs 00, 17 sincronizados |
| 2.77 | 2026-08-12 | **TC-017 refinamientos UX list-tours + tour-details — tourya-front PR #113**. Tres mejoras UX sobre el flujo del turista: (1) **Selector "Ordenar por" en `list-tours`** — 3 opciones exclusivas (Recomendado / Precio menor→mayor / Precio mayor→menor). Sort client-side sobre `tour.priceFrom` (asc/desc); "Recomendado" restaura el orden del backend. (2) **Eliminados los toggles grid/list** junto al selector — el default queda `grid`. (3) **Botón "Volver" en `tour-details`** arriba del breadcrumb — usa `location.back()` con fallback a `routes.listTours` cuando `window.history.length <= 1` (entrada directa por URL). Nuevas keys i18n: `toursList.priceLowToHigh`, `toursList.priceHighToLow`, `common.back` (utility key general reusable). Sin cambios de reglas de negocio — solo UX. TC-017 sigue OPEN esperando validación final de Luis tras deploy. Docs 00, 17 sincronizados |
| 2.76 | 2026-08-12 | **TC-017 fix mapa Hotel Pickup + i18n priceType — tourya-front PR #112**. Tres items en un PR: (1) **Mapa oculto en tours Hotel Pickup** en `tours-detail.component` — el guard `*ngIf="mapCenter"` no bastaba porque el backend puede enviar coords por defecto de la ciudad; fix con getter `isHotelPickup` + `*ngIf="!isHotelPickup"` envolviendo el mapa. (2) **Etiqueta priceType traducida** — `getPriceTypeLabel()` comparaba contra `'INDIVIDUAL'`/`'GROUP'` (mayúsculas) pero backend envía `'individual'`/`'grupo'` (minúsculas) → siempre caía al fallback string crudo. Fix: normalización case-insensitive + nuevas keys `tour.priceType.{individual,group}` en ES/EN/PT + formato `"{label} (N)"` con N = `tour.maxPeople`. (3) **Bug colateral en `tour-grid-view.component`**: usaba `tour?.maxCapacity` (campo inexistente en `TourDto`) → N siempre 0; reemplazado por `maxPeople`. TC-008 #195 cerrado por Luis 2026-08-12 15:33 tras PR #110 (backlog TC-008-fe actualizado). Docs 00, 17 sincronizados |
| 2.75 | 2026-08-12 | **TC-017 mensaje Hotel Pickup en 4 vistas del turista — tourya-front PR #111**. Aviso largo/corto de Hotel Pickup en `tours-detail`, `tour-booking-confirmation`, `list-tours` (grid + list) y `cart-summary` via i18n `tour.hotelPickup.{longMessage,shortMessage}` en ES/EN/PT. Bonus colateral: removidos fallbacks fake en `cart.service.ts` (Cartagena/Bolívar/Colombia/Plaza de la Aduana) que enmascaraban tours Hotel Pickup mostrándolos como si tuvieran dirección — bug preexistente. Cierre esperado del issue #220. Docs 00, 17 sincronizados |
| 2.74 | 2026-08-12 | **TC-008 header duplicado en maritime-reports retirado — tourya-front PR #110**. Eliminado `<h2>Maritime Activity Reports</h2>` hardcodeado en `maritime-activity-reports.component.html`. El shell admin ya renderiza su propio breadcrumb; el header quedaba duplicado cuando se entraba por sub-flujos. Cierre real del issue #195 tras las 2 pasadas previas de sidebar (PRs #96, #109). Docs 00, 17 sincronizados |
| 2.73 | 2026-08-12 | **TC-019 bug 4 — `slotPorcentajeTourya` editable desde config-slot base — tourya-api PR #247 + migración 090**. Luis: al asignar un valor al `slotPorcentajeTourya`, se guardaba pero `price` base no se recalculaba. **Causa**: `TourScheduleConfigSlotDto` NO tenía el campo, entonces ningún endpoint que persistiera el slot base (POST /config, PUT /config/{id}, POST /batch) podía recibir un slotPct explícito. `manageSlotsUpdate` solo actualizaba el pct base al crear slot nuevo (hereda tour default) o al pasar de pct=0 a tour.default>0. Fuera de eso, reusaba el pct existente. **Fix**: campo agregado al DTO (0-100 puntos, nullable), `buildSlotsAndPricesFromRequest`/`manageSlotsUpdate` lo honran cuando el rol es BACKOFFICE (RN-015), `applyPriceDtoToEntity` recalcula desde `providerPrice` (idempotente). No toca overrides per-schedule (`PUT /percentage/{slotId}` que ya funcionaba bien). Migración 090 backfillea 4 filas de drift acumulado en dev (slots 522/526/629 tour 43, slot 527 tour 36). Docs 00, 05 §RN-015, 08 §Fase 10, 09 §TourScheduleController sincronizadas |
| 2.72 | 2026-08-12 | **TC-019 bug 3 — reagendar ajusta booking count del día original y del nuevo — tourya-api PR #246 + migración 089**. Luis: tras un reagendamiento la UI mostraba drift en ambos lados (día viejo con `bookings` inflado, día nuevo con `bookings=0`). **Causa**: helper `fn_slot_booked_units_on_schedule` (introducido en TC-004 mig 078) excluía `RESCHEDULED` del `IN` de delivery_status; el item ya apuntaba al nuevo `(slot, schedule)` pero el helper lo ignoraba. **Fix**: migración 089 recrea el helper agregando `RESCHEDULED` + backfill idempotente en 2 partes (792 filas de `slot.bookings` y 404 de `slot.availability`) usando la definición nueva — complementa mig 080 (BE-27). `ReservationRepository.countActiveBookingUnitsForSlotOnDate` también agrega `'RESCHEDULED'` al `IN` para mantener el guard Java `ensureSlotHasCapacity` sincronizado y evitar oversell en la fecha destino. Verificado en dev: RES 335 (individual, 2 pax, slot 1042) helper 0→2; RES 333 (grupo, slot 1032) helper 0→1; simulación en tx con ROLLBACK OK. **Nueva RN-061** en doc 05. Docs 00, 05, 08 sincronizadas |
| 2.71 | 2026-08-11 | **Sync ciclo QA agosto 2026 (docs)**. Actualización masiva de documentación tras el ciclo de bug-fixes reportados por Luis entre 2026-08-04 y 2026-08-12 (22 PRs backend + 14 PRs frontend). Docs actualizados: **00** (este changelog + entradas v2.60-v2.71), **05** (nuevas RN-057 DIMAR inmutable, RN-058 scrub datos cliente, RN-059 siempre `es` en tour i18n, RN-060 timezone Bogota), **08** (columnas nuevas `tourist_profile.document_type`, `tour_schedule_config.sub_category`, `tour_address` geo NULLABLE + CHECK Hotel Pickup + `HOTEL_PICKUP` en enum + Fase 9/10 migraciones + tabla 062-088), **12** (sidebar admin + reglas layout ADMIN vs BACKOFFICE_OPERATION TC-008), **13** (Dockerfile con `tzdata` + `-Duser.timezone=America/Bogota` + explicación cadena de defensa TZ), **17** (10 TCs nuevos como CERRADO en sección B: TC-009/011/014/015/016/017/018/019/020/021 + TC-008-fe en C). Ver PR de sync |
| 2.70 | 2026-08-11 | **TC-021 backend cerrado + TC-020 completo (Dockerfile TZ) — tourya-api PRs #241 + #244 + tourya-front PRs #105/#106**. **TC-021** (PR #241): scrub de datos cliente cuando el rol es PROVIDER puro y falta más de 1 día para el tour — se ofuscan `payerName/payerEmail/payerPhone/payerDocumentNumber/serviceResponsibleName/Email/Phone`. A partir del día anterior al tour el provider ve todo. ADMIN y BACKOFFICE_OPERATION siempre ven todo. Frontend PR #106 hace mirror en 4 vistas. **Nueva RN-058** en doc 05. **TC-020 real** (PR #244): fix del bug (c) reportado por Luis en #235. Antes Cloud Run corría el container en UTC → cualquier `LocalDateTime.now()` bare (~28 sitios) resolvía a UTC. Cadena de defensa: (1) `Dockerfile` instala `tzdata` + `ENV TZ=America/Bogota`, (2) `ENTRYPOINT` con `-Duser.timezone=America/Bogota`, (3) `JpaAuditingConfig` con `DateTimeProvider` `LocalDateTime.now(BOGOTA)`. Fix parcial previo PR #240 (`ReservationService.LocalDateTime.now(BOGOTA)`). Frontend PR #105 aprovecha TC-009 (documentType) para mandar el `docType` real a Wompi + botón "Ver Reservas" en confirmación. **Nueva RN-060** en doc 05, sección TZ en doc 13 |
| 2.69 | 2026-08-11 | **TC-019 3 regresiones de precio cerradas — tourya-api PRs #243 + #232 + #233 + #238 + frontend PR #104 + migraciones 086/088**. Feature original: nueva columna `tour_schedule_config.sub_category` (migración 086) para filtrar templates por subcategoría del tour desde el combo (PR #233 backend + PR #104 frontend). Hotfix cast enum PG (PR #238): `@ColumnTransformer(write="?::tour_subcategory_enum")` porque JPA no castea el enum automáticamente. **Y 3 regresiones cadena** cerradas por PR #243 + migración 088: (R1) `POST /tour-schedules/batch` guardaba `price = provider_price` porque muchos slots legacy tenían `slot_porcentaje_tourya = 0` y BE-02 solo hereda en slots NUEVOS — backfill de 444 slots al margen del tour; (R2) reagendamiento mostraba `price=providerPrice` porque `SearchTourScheduleSlotPercentageEnricher` ignoraba `slot.slot_porcentaje_tourya` — 783 prices backfilleados; (R3) disponibilidad `bookings=0` porque migración 085 (TC-018b) reescribió `sp_get_tour_schedule_json` usando `sl.bookings` en vez del helper `fn_slot_booked_units_on_schedule` introducido por TC-004 (mig 078) — regresión accidental al copiar el SP, restaurada en 088. Regresión previa del batch 404 resuelta en PR #232. Lección: al reescribir SPs auditar los fixes previos que preservar |
| 2.68 | 2026-08-10 | **TC-018 DIMAR RED — 5 iteraciones antes del fix final — tourya-api PRs #229/#230/#234/#237/#239 + migración 085 + frontend PRs #101/#108**. **Bug A** PR #229: hook DIMAR debe dispararse en `update` (no solo `create`) e incluir `RESCHEDULED` en la búsqueda (además de `PENDING`) + fail-loud si el listener falla. **Bug B** PR #230 + migración 085: expone `blockedByMaritimeReport` en `sp_get_tour_schedule_json` + hard guard en `ShoppingCartService.addItemToCart` que rechaza el add si el frontend logra bypassear. **Iteraciones 3/4/5** (diag + fix intermedio + fix real): hook `@Async @TransactionalEventListener(AFTER_COMMIT)` **nunca se ejecutó** en producción por interacción con `@Transactional`. PR #234 agregó endpoint `POST /maritime-activity-reports/{id}/trigger-alert` + logs INFO para diagnosticar. PR #237 hizo el listener síncrono + instrumentación. PR #239 (fix definitivo): **direct call** desde `MaritimActivityReportService.create/update` a `ReservationService.cancelAffectedByRedAlert()` — bypass total del listener. Lección: eventos AFTER_COMMIT+@Async son frágiles cuando el flujo también es async — a veces el direct call gana. Frontend PR #101 (modal deshabilita días RED) + PR #108 (rediseño columnas listado + botones por flag + fix precarga subcategoría). **Nueva RN-057** en doc 05 (reportes RED son inmutables) |
| 2.67 | 2026-08-11 | **TC-017 Hotel Pickup cerrado — tourya-api PRs #226/#228/#242 + migración 087 + tourya-front PRs #100/#107**. Backend en 3 pasadas: (a) PR #226 agrega valor `HOTEL_PICKUP` a `AddressTypeEnum` + relaja lookups Java de country/state/city; (b) PR #228 guard null en lookups + enum matcher tolerante (el enum guarda `"Hotel Pickup"` display value, no la key `HOTEL_PICKUP`); (c) PR #242 **migración 087** hace NULLABLE `country_id/state_id/city_id` en `tour_address` + `CHECK constraint tour_address_geo_required_unless_hotel_pickup` — solo `address_type='Hotel Pickup'` admite geo NULL. **Bug de proceso**: PRs #226/#228 se mergearon sin la migración → fallaba con `null value violates not-null constraint`; #242 la puso 2 días después. Frontend PR #100 (flag Hotel Pickup en editor + mensajes) + **PR #107 opción B (nueva RN-059)**: siempre guardar campos i18n del tour en slot `es` regardless del idioma UI — evita perder el nombre en español si el PROVIDER creaba el tour con UI en inglés |
| 2.66 | 2026-08-09 | **TC-016 completo — tourya-api PRs #224 + #225 + migración 084 + tourya-front PRs #98 + #99 + #102**. **Parte A**: `TourAddressResponse` expone `countryName/stateName/cityName` para render de meeting point en 4 vistas. **Parte B**: `travelerBreakdown` (array por `ageType` con `quantity`) expuesto en `sp_get_provider_reservations` (migración 084) — el detalle vivía en `shopping_cart_item_detail` pero requería llamadas extra; ahora viene en el mismo response. Frontend renderiza breakdown en modal reserva + floating cart + 4 vistas |
| 2.65 | 2026-08-07 | **TC-015 porcentajeTourya en detalle — tourya-api PR #223 + tourya-front PRs #97 + #103**. Complemento de BE-01/FE-05: `TourFullDataResponse` ahora expone `porcentajeTourya` para que el backoffice vea el valor actual del tour en la vista de detalle (no solo en el modal de aprobación). Frontend PR #97 UI para editar post-aceptación + PR #103 refinamiento lista admin (quitar hero-image del detalle) |
| 2.64 | 2026-08-07 | **TC-011 NO_SHOW job (3 iteraciones) — tourya-api PRs #217 + #221 + #222**. Reabierto 2 veces tras el cierre inicial. (a) PR #217 incluye reservas `RESCHEDULED` además de `PENDING` en el barrido de `PendingReservationNoShowJob` (7am Bogotá); (b) PR #221 aísla la tx de `recalculate` en `TransactionTemplate REQUIRES_NEW` — antes el `recalculate` reventaba y revertía la marca NO_SHOW quedando reservas atascadas; (c) PR #222 endpoint temporal `POST /reservations/no-show-job/trigger` protegido con `Utils.isTouryaBackoffice()` para dispararlo manualmente sin esperar el cron |
| 2.63 | 2026-08-06 | **TC-009 `document_type` en tourist_profile — migración 083**. Antes vivía solo en `payment.payer_document_type`, se re-preguntaba en cada compra. Ahora se persiste en el perfil para pre-llenar futuras compras (varchar 50 nullable, valores `CC/CE/PP/NIT/TI`). Consumido después por tourya-front PR #105 (TC-020) para mandar el `docType` real a Wompi en vez del hardcode `"CC"` |
| 2.62 | 2026-08-11 | **TC-008 sidebar admin persistente + ADMIN mantiene menú completo — tourya-front PRs #96 + #109**. Cierra 2 regresiones que aparecieron al desplegar FE-15b/c (`BackofficeGuard`): (1) sidebar admin se perdía al navegar entre vistas admin; (2) ADMIN veía solo los 3 items del subset BACKOFFICE al entrar a `/admin/bookings-management`. Fix: sidebar persistente en shell + items por rol (ADMIN todo, BACKOFFICE_OPERATION 3 items: reservas, payouts, DIMAR). Doc 12 §Layout admin actualizado con la regla operativa |
| 2.61 | 2026-08-04 | **TC-014 auto-purga cart items vencidos — tourya-api PR #216**. En `GET /shopping-cart` los items con `schedule_date < today (Bogota)` se eliminan silenciosamente antes de responder. Evita que un item quede huérfano cuando el turista dejó el carrito abierto por días |
| 2.60 | 2026-08-04 | **Arranca ciclo QA agosto 2026 tras issues de Luis** — 22 PRs backend + 14 PRs frontend entre 2026-08-04 y 2026-08-12. Bloques temáticos cerrados en changelog: TC-014, TC-011 (3 iter), TC-009, TC-015, TC-016 A/B, TC-017 (3 pasadas + Hotel Pickup opción B), TC-018 (5 iter DIMAR RED), TC-019 (feature + 3 regresiones precio), TC-020 (TZ Bogota real), TC-021 (scrub datos cliente), TC-008 (regresiones layout admin). Ver entradas v2.61-v2.71 abajo |
| 2.59 | 2026-07-24 | **BE-26 cerrado como no-op (auditoría)**. Luis reportó el 19-jul en doc 05 §RN-026 que las reservas con tour GRUPO fallaban tras el pago Wompi con mensaje `"Error en la reserva. El pago fue exitoso pero hubo un error creando la reserva. Por favor contacte soporte"`. Auditoría 24-jul: el mensaje es el catch-all del `handlePaymentResult` en `cart-summary.component.ts:912` — se dispara para **cualquier** error tras Wompi APPROVED, no es específico de GRUPO. Logs Cloud Run dev últimos 14d: los 7 errores 500 en `POST /payment` fueron entre `2026-07-15T21:24Z` y `2026-07-17T00:04Z`, todos causados por bug #179 (migración 075 device_token_fcm faltante). **Desde 2026-07-17 cero errores 500** en `/payment` ni en `PaymentService`. Luis reportó 2 días después del fix (MO-40b + HOTFIX 075) y no probó de nuevo antes de documentar. La lógica GRUPO en `ReservationService:1945/2012/2485` no genera fallos. **BE-26 marcado 🟢 en backlog y en doc 05 §RN-026**. Sin código adicional |
| 2.58 | 2026-07-23 | **HOTFIX #193 — 500 al declinar reserva por JPQL con enum PG mal enlazado (tourya-api PR pendiente)**. Bug crítico reportado por Luis tras probar el botón "Decline" de FE-24: backend respondía 500 al llamar `PUT /reservations/{id}/decline`. **Root cause**: `TourRepository.findAlternativesBySubCategoryQuery` (agregada en PR #197 para BE-24) usaba JPQL con `AND t.subCategory = :subCategory`, pero como `tour.sub_category` es enum nativo Postgres (`tour_subcategory_enum`, migración 016), Postgres rechazaba con `operator does not exist: tour_subcategory_enum = character varying`. El `@ColumnTransformer(write = "?::tour_subcategory_enum")` de `Tour.java:70` solo aplica a INSERT/UPDATE, no al WHERE. La query fallaba dentro del try/catch del `sendProviderDeclinedEmailBestEffort`, pero la tx `@Transactional` padre quedaba marcada como aborted por Postgres → todos los queries siguientes (mapper + enrichReservationResponse) fallaban con `current transaction is aborted` → 500. **Bonus descubierto**: mismo bug latente en `ReservationRepository.findAffectedByRedAlert` (BE-23) — no había explotado porque el evento DIMAR bandera roja no se había disparado en dev. **Fix**: ambas queries convertidas a native con `CAST(:subCategory AS tour_subcategory_enum)` + wrapper `default` que convierte el enum a `getValue()` para no romper callers. `findAffectedByRedAlert` además convierte `List<DeliveryStatusEnum>` a `String[]` para el binding del native query con `ANY(...)` |
| 2.57 | 2026-07-23 | **FE-24 cerrado — UI provider decline en Angular (tourya-front PR #77)**. Cierra la parte web de BE-24 Fase 1: botón "No puedo atender" en la tabla de reservas del provider + modal Swal de confirmación explícita con el impacto completo (cancel + crédito + email alternativas + no se deshace) antes de disparar `PUT /reservations/{id}/decline`. 6 archivos: 3 Angular (`reservation.service.ts` nuevo método `declineReservation`, `booking-management-config.service.ts` nueva `ActionConfig` con id `decline` icono `fa-user-times` warning naranja, `provider-tour-management.component.ts` case `decline` + método privado `openDeclineConfirmModal`) + 3 i18n (`es/en/pt` × 7 keys nuevas). Visibilidad del botón: solo `status ∈ {Pending, Rescheduled}` — mismo criterio que cancel; estados terminales no muestran (backend igual rechazaría con 400). Success dispara `notifyReservationUpdated()` para refrescar la lista. Build production limpio (sexta vez consecutiva con `feedback_ng_build_production`). **Nueva deuda registrada**: FE-24-mobile en backlog D.2 — paridad MAUI del botón, talla XS, no bloqueante (provider puede usar la web) |
| 2.56 | 2026-07-23 | **TC-007 backend cerrado — guard temporal en `consumeReservation` + fix zona horaria del flag (tourya-api PR pendiente)**. Cierra la parte backend de #194: el PROVIDER podía confirmar (marcar `DELIVERED`) una reserva cualquier día que estuviera en `PENDING`, sin validar que fuera el día del tour. Ejemplo real reportado por Luis: RES-318 con `reservationDate = 24 jul 2026` mostraba el botón "Confirmar" desde el 22 jul. **2 fixes**: (1) `ReservationService.consumeReservation` nuevo guard temporal que compara `reservation.getReservationDate().toLocalDate()` con `LocalDate.now(BOGOTA)` — si difieren, `IllegalStateException` → 400 con mensaje `"Only can confirm reservation on the tour day (tourDate=X, today=Y)"`. (2) `computeCanConfirmReservation` línea 1358 usaba `LocalDate.now()` sin zona — bug latente que hacía el flag inconsistente en el borde del día si server está en UTC. Ahora usa `LocalDate.now(BOGOTA)`. Constante `private static final ZoneId BOGOTA = ZoneId.of("America/Bogota")` agregada al service (patrón ya usado en 7 jobs del proyecto). **Nueva RN-056** en doc 05. Frontend queda como PR separado (tourya-front): `mapProviderReservationToBooking` y `mapClientReservationToBooking` deben copiar `canConfirmReservation` al booking — la lógica de show/hide del botón en `booking-management-config.service.ts:300` ya lo respeta, pero los 2 mappers de tabla no lo copiaban, cayendo al fallback por status |
| 2.55 | 2026-07-23 | **TC-008 backend cerrado — PROVIDER no ve `price` en el schedule (tourya-api PR pendiente)**. Cierra la parte backend del bug reportado por Luis en #195: al ver el schedule de sus tours, el PROVIDER veía el `price` (precio cliente) además de su `providerPrice`. **Origen**: contradicción en docs — el doc 05:234 RN-019 decía "el PROVIDER solo omite `slotPercentageTourya`, ve `providerPrice` y `price`" mientras que TC-008 pedía ocultar `price` también. Luis definió la política definitiva (23-jul): PROVIDER solo ve `providerPrice`. **Fix**: 1 línea en `TourScheduleConfigGeneralService.mapSlotToResponse:653` — `priceDto.setPrice()` condicionado a `showTouryaFields = Utils.isTouryaBackoffice(roles)`. PROVIDER recibe `price = null` en el JSON. ADMIN y BACKOFFICE_OPERATION siguen viendo los 3 campos. **Docs sincronizadas**: RN-019 reescrita en doc 05, matriz de doc 03:171 extendida con fila "Ver `price` (precio cliente)". Frontend queda como PR separado (tourya-front, para 2 badges lado a lado en tabla del ADMIN + fix `getAgeTypeDisplay` para AgeType en español + `isTouryaBackoffice` en vez de `isAdmin`) |
| 2.54 | 2026-07-23 | **Fix #189 backend — `priceType` y `maxPeople` expuestos en `ShoppingCartItemResponse` (tourya-api PR pendiente)**. Bug reportado por Luis en re-prueba de TC-006: el resumen del carrito mostraba "2 personas" en vez de "1 Grupo(s) (2 personas)" para tours GRUPO. Root cause: el DTO `ShoppingCartItemResponse` no incluía el campo `priceType`, así que el frontend (`cart.service.ts:882`) recibía `undefined` en `item.priceType`, `item.tour.priceType` salía `undefined` en cart-summary y el `*ngIf="item.tour?.priceType === 'grupo'"` era falso → caía al render de individual. **Fix mínimo**: agregar `priceType: String` y `maxPeople: Integer` al response DTO + setearlos desde `item.getTourSchedule().getTour()` en el único punto de construcción (`ShoppingCartService:668`). Frontend intacto — el mapping ya estaba listo esperando el campo. **Preexistencia**: el DTO nunca tuvo esos campos; el frontend los agregó en el PR #72 de TC-006 asumiendo que backend los enviaba. Cero cambios en tests (no se cubrían) — deuda registrada implícitamente en TC-006 backend contract test |
| 2.53 | 2026-07-23 | **BE-24 Fase 1 cerrada — endpoint provider decline + fix colateral AccountPayable (tourya-api PR #197)**. Cierra el rediseño de RN-055 (Luis 23-jul, issue #193): descartada la reasignación manual del ADMIN, ahora al declinar el sistema automáticamente cancela + genera crédito (reusa BE-23) + envía email al turista con tours alternativos + anula `AccountPayable` (fix colateral de doble pago). Migración `081_be24_reservation_provider_declined_at.sql` aplicada a Cloud SQL dev. Nuevo endpoint `PUT /reservations/{id}/decline` con guards (ownership del provider dueño del tour, estado terminal, ya declinada previamente). Template Thymeleaf `provider_declined_notification.html` con crédito destacado + lista de tours alternativos ordenados por rating de la misma `subCategory`. **Fix colateral BE-23**: descubierto en auditoría que `cancelReservationByRain` y `cancelOneByRedAlert` NO tocaban el `AccountPayable` — el `ProviderPayoutOrderJob` habría causado doble pago (turista con crédito + provider recibiendo su parte). Nuevo helper `voidAccountPayablesForReservation` aplicado en los 3 puntos de cancelación (idempotente: skip si AP ya CANCELLED). Fase 1 no incluye UI para llamar el endpoint — FE-24 queda como item futuro. **Fase 2 (roadmap)**: sistema de penalizaciones por ventana temporal (48h/24-48h/<24h/no-show), talla XL sin timeline. Deuda **BE-24b** registrada para test integrado con Testcontainers junto con BE-20b y BE-23b. FE-13 (UI ADMIN reasignar) marcado como CANCELADO en el backlog |
| 2.52 | 2026-07-22 | **BE-27 cerrado — recalculate en jobs de expiración + backfill drift acumulado (tourya-api PR #192)**. Fix de deuda técnica encontrada durante la auditoría de TC-004: `TemporalReservationExpiryJob` (cada 60s) y `PendingReservationNoShowJob` (diario 7am Bogotá) marcaban reservas como CANCELED/NO_SHOW sin llamar `recalculate`, inflando `tour_schedule_config_slot.bookings` con el tiempo. Post-TC-004 el impacto era mínimo para el usuario (endpoints principales ya calculan en runtime) pero la vista de edición de config del provider aún leía el campo, y cualquier caller futuro también. **Fix**: (a) `expireOne` obtiene `slotId` del item antes de nullear `reservationId` y llama `recalculate(slotId)` con falla aislada — si el recalculate revienta, la cancelación queda persistida; (b) `markNoShows` acumula slotIds en Set durante el loop y ejecuta `recalculate` una vez por slot (dedupe), log ok/failed separados; (c) migración 080 idempotente que backfillea todos los slots (SUM de unidades activas TEMPORAL/PENDING/DELIVERED respetando priceType). **Verificado en Cloud SQL dev**: 33 slots con drift → 0. Slot 509 pasó de `bookings=20/availability=0` a `bookings=0/availability=20` (correcto). 3 tests unitarios nuevos en `TemporalReservationExpiryJobTest` (7/7 verdes total). **Preexistencia**: comentario del job decía "liberación de disponibilidad se recalcula al proximo hold/payment/cancel" — asunción que se rompió con el tiempo. Cerró la deuda que hizo visible TC-004 |
| 2.51 | 2026-07-22 | **TC-006 cerrado — UX diferenciada para tours GRUPO en modal + carrito (tourya-front PR #72)**. Cierra los 4 sub-bugs del issue #189: (1) disponibilidad = 3 en todos los días ya estaba resuelto por TC-004 (PR api #190); (2) input "Cualquiera" mostraba 2 pax en vez de 1 grupo por default; (3) precio total no reflejaba la cantidad correcta; (4) carrito mostraba "N personas" sin cantidad de grupos. **Descubrimiento importante**: Cristian ya había implementado la infra correcta en el modal (`groupCount`, `updateTotals`, validaciones, bloque UI condicional) en commit `32777c6` de jun-2026, pero **dejó el bloque `participants-list` renderizándose también para grupo**, creando dos entradas de cantidad duplicadas que confundían al usuario. **Fix**: (a) `*ngIf="priceType !== 'group'"` en el bloque `participants-list` del modal; (b) summary del modal muestra "Cantidad de grupos: N" en vez de "Total participantes" cuando es grupo; (c) `cart-summary` muestra "N Grupo(s) (M participantes)" y oculta el breakdown por edad para grupo; (d) extender `CartItem.tour` con `priceType?/maxPeople?` opcionales + populate desde `cart.service`; (e) nueva key i18n `cartSummary.groupsLabel` en es/en/pt. Cero cambios en backend, cero cambios en payload al API. Build production verde primera corrida (cuarta vez consecutiva con la regla FE-15b). **Preexistencia**: bug del frontend Angular (Cristian, jun-2026). Ningún commit reciente de nuestro equipo lo introdujo. Con este PR + PR #190 (TC-004) + PRs #191/#71 (TC-005), los 3 issues (#187, #188, #189) que Luis reportó quedan resueltos |
| 2.50 | 2026-07-22 | **TC-005 frontend cerrado — modal detalle muestra precio-proveedor para rol PROVIDER (tourya-front PR #71)**. Complemento del PR api #191 ya mergeado. Nuevo helper `pickBookingPriceForRole` en `provider-tour-management.component.ts`: rol PROVIDER puro (`isProvider() && !isTouryaBackoffice()`) → muestra `providerPrice` ($300,000 tras el fix del SP); ADMIN/BACKOFFICE_OPERATION/CLIENT → mantienen `shoppingTotalPrice` ($345,000, el monto que paga el cliente). Aplicado al `mapReservationToBooking` genérico (línea 396) usado por `viewBookingDetails`; los otros 2 mappers (`mapProviderReservationToBooking` línea 597 usa `providerPrice` directo — ya se auto-corrigió con el fix del SP; `mapClientReservationToBooking` línea 651 usa `shoppingTotalPrice` correctamente). Cero cambios en HTML, cero cambios en modelos. Build production verde en primera corrida (tercer PR consecutivo con la regla FE-15b aplicada). Con ambos PRs (#191 backend + #71 frontend): lista PROVIDER y detalle PROVIDER muestran $300,000, ADMIN/BACKOFFICE muestran $345,000 en todos lados. TC-005 completo, cerrado issue #188 |
| 2.49 | 2026-07-22 | **TC-005 backend cerrado — providerprice sin multiplicar por pax en tours grupo (tourya-api PR #191)**. Fix del bug reportado por Luis en #188: el SP `sp_get_provider_reservations` calculaba `providerprice` como `SUM(provider_unit_price × quantity)` sin distinguir `t.price_type`. Para tours GRUPO el precio del proveedor es fijo por reserva, así que RES-316 (grupo, 4 pax) mostraba $1,200,000 y RES-317 (grupo, 2 pax) mostraba $600,000 en la LISTA "Mis Reservas" del rol PROVIDER (el `mapProviderReservationToBooking` en Angular usa `reservation.providerPrice` como precio mostrado). **Fix**: migración 079 con `CASE WHEN t.price_type = 'grupo' THEN 1 ELSE quantity END` en el `SUM` del providerprice — único cambio vs mig 061. Cero cambios en Java, cero cambios en el frontend (auto-corregido con el fix del SP). Verificado en Cloud SQL dev: RES-316 y RES-317 ahora devuelven `providerprice = 300,000` (cliente sigue pagando $345,000). **Preexistencia**: bug de mig 061 (Donnaly, ~jun 2026). El bug 5b (modal detalle para PROVIDER debe mostrar precio-proveedor, no precio-cliente) queda para PR frontend separado — el campo `providerPrice` ya existe en `ReservationDetailsResponse` y con el fix del SP viene poblado correctamente; solo hay que condicionar el binding en el modal por rol |
| 2.48 | 2026-07-22 | **TC-004 cerrado — bookings/availability por (slot, fecha) en runtime (tourya-api PR #190)**. Fix del bug reportado por Luis en issue #187: el campo `tour_schedule_config_slot.bookings` era un contador global a nivel de config-slot (plantilla), y como el mismo slot config se aplica a múltiples fechas, las 2 reservas del 24-jul aparecían en los 16 días del calendario mensual del tour 72 con `availability=3` (debía ser 5 en 15 de los 16 días). **Confirmado por Donnaly** vía WhatsApp 21-jul: *"si la regla de negocio es cupo por día, hoy el backend no lo hace así — haría falta contar por slotId + scheduleDate"*. **Fix**: (a) nueva query nativa `ReservationRepository.countActiveBookingUnitsForSlotOnDate` que respeta priceType (grupo=1 unidad, individual=suma pax); (b) refactor de `ensureSlotHasCapacity` para exigir `scheduleDate` (3 callers actualizados); (c) `mapSlotToResponse` acepta `scheduleDate` opcional y calcula en runtime cuando viene poblada (via `findAllByTourId`); (d) migración 078 con función helper `fn_slot_booked_units_on_schedule` + reescritura de `sp_get_tour_schedule_json` y `sp_count_tour_schedule_json` para usar el helper en lugar de `sl.bookings`. El campo denormalizado queda intacto por compat pero deja de ser fuente de verdad. Verificado en Cloud SQL dev: slot 1030 del tour 72 ahora muestra `bookings=2` sólo el 24-jul, el resto de días vuelve a `bookings=0/availability=5`. 12 tests unitarios nuevos protegen el fix (incluye regresión explícita del bug: fechas independientes + ignora `slot.bookings` global). **Preexistencia**: bug de diseño desde la migración 017 (Donnaly, 2026-03-30). Ningún commit reciente de Franklin/agente lo introdujo. Bonus resuelto colateralmente: sub-bug 1 de TC-006 (disponibilidad heredada en la vista turista) queda cerrado automáticamente por este PR. Siguen pendientes TC-005 (precio grupo + providerAmount por rol) y sub-bugs 2/3/4 de TC-006 (UX frontend grupo, ver comentario del issue #189) |
| 2.47 | 2026-07-21 | **FE-15c cerrado — UI reportes DIMAR expuesta al backoffice (tourya-front PR #70)**. Auditoría reveló que la UI (`maritime-activity-reports.component.ts`) YA existía pero embebida en dashboard admin bajo `AdminGuard` → BACKOFFICE no la veía aunque FE-15b abrió el backend. Trabajo real: nueva ruta `/admin/maritime-reports` con `BackofficeGuard` (lazy `loadComponent`, componente ya standalone), enlace de navegación desde `bookings-management` (visible solo si `isTouryaBackoffice() && url.includes('bookings-management')`), **modal de confirmación explícita** cuando `flag=RED` — evita que un click descuidado dispare BE-23 (cancelación retroactiva de reservas activas + créditos compensatorios), i18n es/en/pt para el modal y el enlace. Sin cambios en la lógica del componente ni regresión en el embed del dashboard. Pipeline verde en primera corrida (segunda vez consecutiva que el aprendizaje de `--configuration=production` local previene un hotfix). Talla real XS — audit-before-propose evitó estimarlo como S. Confirmado con Franklin que no hay operarios externos por ahora, todo interno, alcance según doc de Luis |
| 2.46 | 2026-07-21 | **FE-15b cerrado + fix H2 seguridad DIMAR (tourya-api PR #186 + tourya-front PR #69)**. Cierra el andamio FE-15 con el reemplazo por pantalla del subset P2 opción C (Luis 18-jul). Backend: `ProviderPayoutOrderService` (3 métodos) migra a `Utils.isTouryaBackoffice()` — cubre "subir pagos". `MaritimActivityReport.create/update/delete` requieren `Utils.isTouryaBackoffice()` — **cierra agujero preexistente H2** donde cualquier autenticado podía crear/borrar reportes DIMAR. Frontend: nuevo `BackofficeGuard`, guards explícitos por ruta (`AdminGuard` para dashboard + tour-admin, `BackofficeGuard` para bookings-management), 5 componentes migrados a `isTouryaBackoffice()`, `home-redirect` de BACKOFFICE → `/admin/bookings-management`. Pipeline del front verde en primera corrida (aprendizaje del hotfix #68: siempre `ng build --configuration=production` local antes de mergear) |
| 2.45 | 2026-07-19 | **BE-22b + BE-22c + BE-22d cerrados (tourya-api PR #185 + tourya-front PR #67)**. Respuesta completa a P1 de Luis. **Backend**: endpoint `GET /provider/users/tour/{tourId}` con ownership check + migración `077_user_phone.sql` aplicada a Cloud SQL dev + `_user.phone` en entity/DTOs/service + fallback retrocompatible en `TourPrincipalOperatorService.fromOperatorLink()` (`user.phone ?? provider.phone`) → impacta `tourOperator.phone` en reservas (RN-026). **Frontend**: nuevo `TourPrincipalOperatorModalComponent` con radio + badge "Actual" + case sin operadores; link "Contacto principal" en el card del tour-list; input `phone` en create/edit de `provider-users`; migración completa de labels hardcoded del tour-list template a i18n en es/en/pt (deuda vieja resuelta de paso). Sigue pendiente **BE-22d-mobile** (paridad opcional en MAUI, XS) |
| 2.44 | 2026-07-19 | **Cierre issue #179 (TC-001) + BE-23 mergeado + respuestas P1/P2 Luis + análisis doc 18**. Issue #179 cerrado formalmente con resumen de los 3 PRs que lo resolvieron (075 aplicada + INF-04 + MO-40b) + BE-23 como bonus. BE-23 (DIMAR bandera roja retroactivo) mergeado en PR #184. Respuestas Luis WhatsApp 19-jul: **P1** — el selector de contacto principal va como botón en el card del tour + agregar campo `phone` en `PROVIDER_OPERATOR` (nuevos items **BE-22b/c/d**). **P2 opción C** — BACKOFFICE_OPERATION subset: ver reservas, subir pagos, gestionar reportes DIMAR (queda como **FE-15b**). Luis además reportó en RN-026 el bug de compra con tipo GRUPO (**BE-26**, distinto a #179) y creó **doc 18 Códigos de descuento partners B2B** — feature grande (**BE-30..38**, ~2-3 semanas, riesgo alto en fase 2 por tocar `processPayment`). Backlog sincronizado con todos los items nuevos. Espera respuesta Luis sobre BE-26 (¿bug real o mensaje escrito antes del fix?) |
| 2.43 | 2026-07-19 | **FE-15 andamio Angular BACKOFFICE_OPERATION (tourya-front PR #66)** + **auditoría BE-23/24 backlog honesto**. FE-15 andamio: `Roles.BACKOFFICE_OPERATION = 35` (id confirmado por query BD) + `AuthService.isBackofficeOperation()` + `AuthService.isTouryaBackoffice()`. Aditivo, no reemplaza `isAdmin()` (el reemplazo por pantalla queda como **FE-15b** bloqueado por P2). BE-23 auditado: existe infra (entidad, endpoint POST, enum, uso defensivo en `ReservationService:1289`), falta hook retroactivo `AFTER_COMMIT` que cancele reservas + genere créditos al crear reporte RED (patrón MO-40b, ~S). BE-24 auditado: feature 100% nueva sin infra previa (talla M, riesgo medio por cambio de `tour_id` en reserva viva) |
| 2.42 | 2026-07-19 | **Auditoría BE-21/22/25 + FE-14 → cerradas como 🟢 sin código**. Aprendizaje del patrón audit-before-propose: los 4 ítems del backlog descritos como "features nuevas" de las observaciones de Luis (17-jul) ya estaban implementados por Donnaly en migración `044_provider_users_roles_and_tour_assignment.sql` (~feb/2026) y en el frontend Angular. Cerrados en backlog: **BE-21** (asignar tours), **BE-22 backend** (endpoint `/principal-tour`), **BE-25 backend** (`Utils.isTouryaBackoffice` + SP guard), **FE-14** (UI ya existente en `provider-users.component`). Abiertos con nombres honestos: **BE-22b** (UX del selector "contacto principal" desde vista del tour — RN-009 reescrita por Luis, P1 pendiente), **FE-15** (Angular reconocer BACKOFFICE_OPERATION — hoy `roles.enum.ts` no lo tiene, P2 parcialmente pendiente). P1 y P2 preguntas enviadas a Luis vía WhatsApp por Franklin. Doc 05 sincronizado con edits de Luis (18-jul) que eliminan la línea duplicada de RN-008 y reescriben RN-009 |
| 2.41 | 2026-07-18 | **TC-002 + TC-003 cerrados (tourya-front PR #65)**. Ambos preexistentes (jun/2025 y may/2026 vía git blame). **TC-002**: título galería `[object Object]` — el template `<h5>Gallery {{tour?.name}}</h5>` nunca se actualizó desde que `tour.name` pasó a `TranslatedField` en dic/2025 (`c11a22b` de Cristian). Fix con `i18nService.getValue()` + 4 claves i18n × 3 idiomas. **TC-003**: modal "Ver reserva" con 6 campos rotos por desfase de contrato — `mapReservationToBooking()` leía nombres del DTO viejo (`ReservationResponse`) pero el endpoint que consume el modal es `GET /reservations?filter` que devuelve `ReservationDetailsResponse` del SP con nombres distintos (`reservationCreatedDate`, `scheduleDate + slotTimeStart`, `totalTourists`, `shoppingTotalPrice`, `reservationDeliveryStatus`, `serviceResponsibleName/Email/Phone` planos). Fix solo en el mapper del `.ts` — template intacto |
| 2.40 | 2026-07-17 | **Observaciones de Luis en docs 03 y 05 → backlog nuevo**. Luis editó `03-roles-y-actores.md` y `05-reglas-de-negocio.md` directo en develop. 5 nuevos requerimientos convertidos en items backlog: **BE-21** asignar tours a `PROVIDER_OPERATOR`, **BE-22** contacto principal por tour, **BE-23** job DIMAR bandera roja → cancelación masiva + créditos (usa MO-40b), **BE-24** reasignar reserva ADMIN + `provider_declined_at`, **BE-25** verificación cross-provider view del ADMIN. Y 3 items front: **FE-12** geocoding Google Maps direcciones (San Andrés), **FE-13** UI reasignar reserva, **FE-14** UI asignar tours a operador. 2 preguntas pendientes para Luis: frase cortada "asignar la re" en responsabilidades ADMIN + confirmar columna BACKOFFICE en tabla de permisos. Los TC-002/TC-003 del front quedan como preexistencia a resolver después |
| 2.39 | 2026-07-17 | **OP-179c + audit de migraciones pendientes en dev**. Al ver que INF-04 aún dejaba 5 reservas huérfanas fallando, apareció OTRA columna faltante (`credit.expired_notified_at` de migración 074, BE-18/19). Auditadas 066-076: faltaban **074 y 076** (esta última no rompía porque IA-00 aún no invoca el repo). Aplicadas ambas manualmente. Job barrió las 5 restantes en la siguiente corrida (`Expiradas 5 (fallidas: 0)`). Wompi de dev es sandbox → sin refunds. **Deuda mayor abierta INF-05**: el pipeline de deploy no corre migraciones; ya vamos por la 3a que se salta. Hay que meter Flyway/Liquibase o Cloud Build step antes de que la próxima nos pare producción |
| 2.38 | 2026-07-17 | **MO-40b — push hooks a `@TransactionalEventListener(AFTER_COMMIT)` + `@Async`**. Cierra por diseño la clase de bug de #179: los push de Fase D ya no viven dentro del `@Transactional` de negocio. Nuevo package `services.push` con `PushDomainEvent` (sealed + 6 records snapshot) y `PushDomainEventListener` con `safeSend`. 5 flows migrados (PaymentService, ReviewService, TourReminder24hJob, CreditExpirationJob x2). 8 tests nuevos + 7 adaptados. Ningún fallo del stack de push puede tumbar la tx de negocio |
| 2.37 | 2026-07-17 | **HOTFIX #179b — `TemporalReservationExpiryJob` resistente a `itemId` NULL**. El job entraba en loop de `UnexpectedRollbackException` desde el 15-jul: encontraba 8 reservas TEMPORAL huérfanas con `item_id = NULL` (efecto colateral del FK `ON DELETE SET NULL` de migración 012 disparado por retries post-500 de MO-40). Fix: guard cuando `itemId == null` + cada iteración corre en `TransactionTemplate` `REQUIRES_NEW` para aislar fallos por reserva. 4 tests JUnit verdes. Explicación completa del árbol causal en el issue #179 |
| 2.36 | 2026-07-17 | **HOTFIX #179 — migración 075 aplicada a Cloud SQL dev**. Luis reportó POST /payment 500. Root cause: MO-40 Fase A creó `075_device_token_fcm.sql` pero nunca corrió en dev; Fase D consulta `device_token` dentro de `@Transactional` y el error de Postgres marca la tx como rollback-only → 500 al cliente y reservas pegadas en TEMPORAL. Aplicada manualmente vía psql en Docker. Deuda IA-40b abierta: mover hooks push de Fase D a `@TransactionalEventListener(AFTER_COMMIT)` para que ninguna falla de push tumbe pagos |
| 2.35 | 2026-07-17 | **MO-50 + MO-51 operario mobile cerrados**. Offline cache Dashboard con JSON + banner. Vista reservas con chips filtro por tour + agrupación por día. Cierra bloque operario mobile |
| 2.34 | 2026-07-16 | **IA-01 framework shared agentes + rescate IA-00**. Package agents.shared con ILlmClient/AnthropicClient/AgentLlmResponse/ModelPricing/PromptTemplate/AgentRunResult/AgentAuditWriter/BudgetGuard. IA-00 rescatado via reflog. 13 unit tests. Framework listo, activación cuando se suba ANTHROPIC_API_KEY a Secret Manager |
| 2.33 | 2026-07-15 | **IA-00 tabla agent_audit_log**. Arranca bloque IA. Migración 076 + entity JPA + repository + 3 índices. Habilitador de IA-01 y siguientes |
| 2.32 | 2026-07-15 | **MO-31 + MO-34 refinamiento provider mobile cerrados**. Compresión imágenes con SkiaSharp (~85% ahorro) en fotos de reseña + copiar de otra plantilla en ScheduleTemplateForm |
| 2.31 | 2026-07-15 | **FE-11 i18n `/providers/requestproviders` cerrada**. ~50 claves nuevas en es/en/pt.json + HTML/TS con pipes translate. Cierra deuda del mismo día |
| 2.30 | 2026-07-15 | **MO-43 deep-linking WhatsApp cerrado**. Share sheet nativo + IntentFilter capture. Sin autoVerify (deuda MO-43b). Constants.WebBaseUrl para prod switch |
| 2.29 | 2026-07-15 | **MO-42 + MO-41 geo mobile cerrados**. Cómo llegar (open external map) + Cerca de mí (Haversine + toggle). Backend intacto |
| 2.28 | 2026-07-15 | **MO-21 PayoutsPage provider mobile cerrada**. Lista con 4 totales + filtro status + detalle con reservas + descarga comprobante via Launcher. Cierra ciclo económico provider en mobile. Backend intacto |
| 2.27 | 2026-07-15 | **MO-40 Fase D backend hooks push — MO-40 COMPLETO**. 5 flows integrados: reserva confirmada (turista+provider), recordatorio 24h (nuevo job), crédito por expirar/expirado, review respondida. `PushNotificationService` con 6 helpers de dominio. Tests 21/21 verdes |
| 2.26 | 2026-07-15 | **MO-40 Fase C mobile recepción + activación real del backend push**. Secret Manager wired a Cloud Run (rev `00134-ffz`, Firebase Admin SDK inicializado). Handler mobile con foreground alert + bg deep-link. Falta Fase D (hooks en flows) |
| 2.25 | 2026-07-15 | **MO-40 Fase B mobile registro FCM**. Plugin.Firebase.CloudMessaging + google-services.json + AuthService hooks. Fire-and-forget al login, DELETE antes del logout. Errores no bloquean auth. Faltan Fases C (recepción) y D (hooks backend) |
| 2.24 | 2026-07-15 | **MO-40 Fase A backend push (FCM)**. Nueva tabla `device_token`, endpoints `POST/DELETE /users/device-token`, `PushNotificationService` con Firebase Admin SDK 9.4.0. Modo no-op si no hay credentials (dev/CI). Decisión: reusar proyecto Firebase `tourya-169d6`. Docs 00, 11, 17 sincronizados. Faltan Fases B/C mobile y D hooks |
| 2.23 | 2026-07-15 | **MO-12 UI créditos + transferencia mobile cerrada**. Saldo + 3 secciones + lookup por documento + transfer con confirm. Primera UI de créditos en cualquier cliente Tourya (web nunca la tuvo). Backend intacto |
| 2.22 | 2026-07-15 | **MO-22/23/24 operarios mobile cerrados en bloque**. CRUD (list + create + edit + reset password) + share sheet nativo para compartir credenciales. Nuevo tile en Dashboard provider. Backend intacto. Docs 15 y 17 sincronizados. Código mobile sigue en local |
| 2.21 | 2026-07-15 | **MO-11 Wishlist mobile cerrada**. UI con lista paginada + toggle ❤️ desde TourDetail. Backend intacto. Nuevo overload `ApiService.DeleteAsync<T>` para el DELETE con body. Docs 15 y 17 sincronizados. Código sigue en local (MO-00 postponed) |
| 2.20 | 2026-07-14 | **HTTPS habilitado en LB dev + CORS whitelisting de `dev.tourya.co`**. Cert Google-managed `tourya-dev-cert` + forwarding rule 443 sobre la misma IP del LB (mismo url-map, mismo routing). Backend agrega `https://dev.tourya.co` + `http://34.160.22.16` a `allowedOrigins`. Cierra la deuda de "LB dev sin HTTPS" abierta el 2026-07-10. Habilita el path para mergear FE-02 (SEC-06 Fase B). Facebook queda pendiente del owner (Luis) |
| 2.19 | 2026-07-13 | **SMTP restaurado + Fase 1 — MO-01/MO-20 cerrados**. Nuevo app-password de Luis aplicado como versión 3 del secret, correo verificado end-to-end contra Hotmail. MO-01 (URL backend al LB) + MO-20 (responder reseñas con fix de multipart) cerrados. Deudas registradas: `_user.email` VARCHAR(50) muy corto, SPF con typo en GoDaddy, DNS A record de Luis en root en vez de subdominio. **Riesgo abierto**: código MAUI sigue sin repo Git |
| 2.18 | 2026-07-12 | **Fase 1 — 17vo bloque cerrado (BE-18/BE-19 combinados)**: notificaciones automáticas de expiración de créditos (RN-036). Nuevo `CreditExpirationJob`, nuevo status EXPIRED, migración 074 con backfill, 2 templates Thymeleaf, 7 tests JUnit. Docs 00, 05, 17 sincronizados |
| 2.17 | 2026-07-11 | **Fase 1 — 16vo bloque cerrado (último BE)**: BE-20 auditoría de reschedule (RN-033) + fix del guard `newDate` + primera suite JUnit real del proyecto (14 tests). Deuda BE-20b (test integrado con Testcontainers) registrada como no bloqueante. Docs 00, 05, 17 sincronizados |
| 2.16 | 2026-07-10 | **Fase 1 — 15vo bloque cerrado (quinto FE)**: FE-05 UI del `porcentaje_tourya` en modal de aprobación del ADMIN. Habilita el DTO existente desde BE-01/02 y agrega el endpoint PATCH. RN-015 pasa de "hoy solo por API" a "UI implementada". Docs 00, 05, 09, 17 sincronizados |
| 2.15 | 2026-07-10 | **Auditoría RN-021 — BE-03/04/FE-07 marcados como cerrados sin más código**. Al ir a implementar el combo se descubrió que la migración 029 del 2026-04-08 ya había eliminado las columnas y el resto del código (backend Java + frontend Angular) también había sido actualizado en su momento. Solo el backlog había quedado desactualizado. Docs 00, 04, 05, 08, 17 sincronizados. Deuda residual documentada: SP `sp_get_tour_schedule` sigue siendo dead SQL que referencia columna inexistente pero nadie lo llama |
